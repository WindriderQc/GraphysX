Original prompt: Bring back the creative GraphysX/BallZ archive ideas in a modern web prototype, especially the old 3D/2D visuals, physics, BallZ game, maps, moving parts, lighting, shaders, and archive-inspired scenes.

Progress:
- Current turn target: move the web prototype closer to the provided TV3D level screenshots with square arena maps, arrow/checker tiles, strong platform edges, white ball patterns, red/blue markers, and clearer old-school lighting.
- Corrected art direction after user feedback: use real archived PNG/JPG/BMP assets instead of generated replacement textures.
- Copied archive candidates into public/assets/textures/archive: yellowtwoway.jpg, yellowtwowaysign.jpg, xboxes03.jpg, wood.jpg, woodsample.jpg, 4Way.png, 5-way.jpg, ARROW.BMP.
- Rewired Archive Level materials to real assets: yellowtwoway for arrow platforms, Damier for checker floors, GridXL for purple/grid base, wood for gold walls, xboxes03 for dark walls.
- Keep generated canvas textures only as fallback/legacy for materials without a suitable source asset.
- BallZ control model corrected: inner ball is now a controller indicator, not a rolling visual. It returns to center at zero force, points forward by default, and points toward the current user force while offset by control magnitude.
- Outside shell now receives physical force via Cannon body.applyForce instead of direct velocity target override. Ground grip and max speed are applied as tuning guards.
- Shell/grid visual roll is derived from actual frame-to-frame displacement, not constant spin or raw velocity, so standing still does not spin in place.
- Re-read BallZ archive mechanics in `GraphysX_1/ZombieKiller.*`, `Archive/bckup/BallZ2015.bckup/CLBallZ.*`, and `Camera.cpp`: the inside ball is a visual controller that looks at the force target, while the outside shell owns the physics.
- Camera model corrected from the archive split: mouse drag/wheel is only for map visit/exploration outside an active race; active races now use an automatic elevated follow camera tied only to outside shell velocity, with camera distance increasing with shell speed.
- Inner force indicator corrected again: removed the visible overlay arrow, kept the archived `FireArrow800.jpg` texture as the visible controller indicator, and align that inner-ball texture to the current user force heading.
- Gameplay UI copy now says the camera follows BallZ during races and only advertises drag/wheel camera outside races.
- In-app Browser currently does not expose window.__GRAPHYSX_DEBUG__ through its evaluate context even though the bundle contains the hook; visual/browser QA and build are still passing.
- Recovered the broader legacy screen branches as active web modes:
  - BallZ Race remains the playable default.
  - Scene Lab previews skyboxes, heightmap terrain, water, sun/moon orbit, spline line, and airplane proxy from `Sky.cpp`, `Atmosphere.cpp`, heightmaps, `Spline.xml`, and airplane sources.
  - Math Lab ports the `MathGameScreen` A/B/C/M/X slider idea into live controls that rebuild a 3D formula/molecule preview.
  - Editor Lab previews the old load/save/clear/add-mesh editor workflow with primitives, selected object frame, piston, and rotator stand-ins.
  - Asset Catalog tracks recovered texture, shader, XML, BallShell/BallCtrl, Suzanne, and Airplane assets with current status.
- Added `src/archive-content.ts` as the registry for recovered screens and asset/status notes.
- Added extra archived texture files under `public/assets/textures/archive`: `Track.jpg`, `heightmap8.jpg`, `flame1.jpg`, `flame4.jpg`, `flameball.jpg`, `blue_flare.jpg`, `ElectronicBoard_fullsize.jpg`, and `concrete.png`.
- Verified `npm run build`.
- Verified the local app in Browser at `http://127.0.0.1:5173`: mode switching works, the canvas remains valid, and no app console errors were observed.
- Captured desktop/mobile Playwright canvas screenshots for Race, Scene Lab, Math Lab, Editor Lab, and Asset Catalog in `output/playwright/`; all passed nonblank pixel-variance checks.
- Current turn target: rethink all BallZ race layouts as level design, especially obstacle placement, path readability, ring placement, and bad-looking ring visuals.
- Replaced clumped/blocked ring pickup layouts in all four race definitions with route-shaped checkpoint lines that guide from start toward the halfway gate and back toward finish.
- Retuned race hazards around designed moments: Level 1 left turn/center choke/home stretch, Level 2 lower maze/center/top corridor, Level 3 platform sweep/center piston/right platform, and Skybox Spiral crosswind/rotator beats.
- Replaced placeholder white sphere pickups with layered glowing torus checkpoint rings, including per-ring yaw and scale metadata for better path readability.
- Follow-up visual fix: added a faint perpendicular loop to each ring so oblique chase-camera angles no longer collapse the pickup into a thin sliver.
- Verified `npm run build`.
- Verified with the develop-web-game Playwright client on the default race; state output still reports gameplay, ring count, player position, velocity, halfway flag, and no script failure.
- Verified all four race openings in the in-app Browser and captured screenshots under `output/layout-qa/`; browser console had no warnings/errors after the refresh.
- Corrected rebuild scope after user clarification: keep the BallZ work already done, and revive the rest of the old GraphysX archive beside it.
- Reframed the web prototype so `Scene Lab` is still the default first screen for the broader archive revival, while `BallZ Race` remains a visible recovered mode with its playable race menu intact.
- Added `Game Basics` mode for non-BallZ gameplay foundations: actor proxies, zombie-style targets, mouse-pick aim line, projectile lane, trigger ring, and flame billboards from the old `Zombie`, `Agent`, `shotBullet`, `Effect`, and particle ideas.
- Added `CubX Lab` mode for the Atmel/CubX-era 3D interface: central CubX proxy, selectable cube buttons, Earth/grid sphere, return arrow, segment-style clock display, and light/fan control panels.
- Updated archive recovery cards and routing so `Next Lab` cycles through Scene Lab, Game Basics, Math Lab, Editor Lab, CubX Lab, and Asset Catalog.
- Verified `npm run build`.
- Verified the local app in Browser at `http://127.0.0.1:5173`: Scene Lab, Game Basics, Math Lab, Editor Lab, CubX Lab, and Asset Catalog all rendered non-empty canvases with no browser warnings/errors.
- Added a first usable `BallZ Map Editor` recovered mode.
- The editor has a 7-tool palette: floor, wall, start, ring, halfway gate, finish gate, and hazard.
- The editor exposes an 11x11 clickable tile grid with single-instance cleanup for start/half/finish tiles, compact ASCII-style JSON export rows, and a live Three.js preview generated from the same serializable draft.
- The 3D map editor preview renders floor tiles, wall blocks, BallZ start marker, torus rings, half/finish gates, and hazard bars.
- Verified `npm run build`.
- Verified the map editor in Browser at `http://127.0.0.1:5173`: the `BallZ Map Editor` mode opens, shows 7 tools and 121 cells, painting a ring updates the export rows and preview, and the browser console reports no warnings/errors.
- Current turn target: replace the incoherent all-in-one main menu, remove hard progression locks, make challenge selection obvious, and stabilize BallZ/camera controls.
- Baseline browser audit found the main faults: the race preview was covered by the rotating menu cube, the fresh-save `Next Race` action looped back to race 1 because every other race was locked, core race selection sat below an oversized recovered-screens catalog, and gameplay exposed no restart/exit action.
- Control pass in progress: movement is now camera-relative, mouse/touch drag orbits the race camera, wheel zoom works during races, `C` resets the camera, jump uses a press edge instead of auto-bunny-hopping while Space is held, and input clears on window blur/start to prevent stuck controls.
- Split the old `"menu"` 3D state into a cube-only home preview and a clean `"race-preview"` world view so menu geometry no longer obscures selected race worlds.
- Verified `npm run build` after the scene/input state changes.
- Rebuilt the application shell around explicit states: compact GraphysX home, separate BallZ Tour challenge selector, gameplay HUD, after-race results, and compact archive-lab navigation.
- Removed hard race locks and the Unlock All workaround. All nine challenges are selectable immediately; numbered order, completion count, medals, and career score now communicate optional progression.
- Added challenge briefs for every race (chapter, difficulty, objective, focus, target, status, record) and automatically scroll the panel back to the Start action after selecting a lower card.
- Gameplay now always exposes Restart Race and Exit To Tour; keyboard equivalents are `R` and `Esc`. Editor-draft races return to the map editor instead of the regular tour.
- Cleared countdown/finish overlays whenever the app returns to a menu/preview state, preventing race-only glyphs from leaking into other 3D views.
- Added a fallback grounded check/vertical-velocity stop for flat arena floors so jumping reliably rearms and the ball no longer accumulates downward velocity while clamped to the floor.
- Added a data-URI favicon so browser QA no longer reports a favicon 404.
- Full-browser QA passed at 1440x900 and 390x844 for Home, BallZ Tour, a directly selected Challenge 5, active gameplay, and Scene Lab. All challenge cards were enabled and the selected challenge preview was free of the menu cube.
- Camera/input QA passed: dragged camera from yaw 0 to -1.2, Forward moved the ball along the new camera-forward direction, `C` returned yaw to 0, and `Esc` returned to `race-select`.
- Final develop-web-game client run passed `home -> tour -> gameplay -> forward/right/jump`; `render_game_to_text` reported gameplay, player/velocity, rings, camera state, coordinate system, and controls with no captured console/page errors.
- Final verification: `npm run build` passes. Remaining output is only Vite's existing large-chunk advisory for recovered legacy datasets.
- Current turn target: complete and balance all nine challenges end-to-end, beginning with deterministic objective/finish verification.
- Found and fixed a legacy-race start blocker: Suzanne, World 1, Map 1, and Piste Ovale could be started before their lazy geometry/car chunks arrived; because active races intentionally do not rebuild on late loads, this could leave a permanently empty challenge.
- Added explicit race asset readiness/error state. The Tour disables Start and shows "Preparing recovered level geometry…" until required chunks are ready, supports retry after a failed import, and refreshes automatically when the data arrives.
- Added deterministic QA state/actions behind `window.__GRAPHYSX_DEBUG__`: race IDs, race selection, start, live objective completion, load state, remaining ring positions, active zombie positions, and active/finished flags.
- Extended `render_game_to_text` with readiness, objective entities, and race lifecycle flags so all nine completion chains can be tested without bypassing the live ring/gate/NPC/scoreboard callbacks.
- Verified `npm run build` after the readiness and QA-hook pass.
- Ran the deterministic nine-challenge matrix. Every race loaded, started, accepted real forward input, completed its live objective chain, entered `after-race`, and displayed `Race Complete`.
- Verified objective totals: Level 1 16/16 rings, Level 2 17/17, Level 3 16/16, Skybox Spiral 16/16, Suzanne 12/12, World 1 9/9, Map 1 10/10, Piste Ovale 10/10, and ZombieKiller 10 zombies defeated.
- The 450 ms movement probe produced nonzero travel in every challenge (1.8 m minimum for the Impreza, 6.541 m maximum for Level 1), ruling out frozen controllers/vehicles at spawn.
- Fixed ZombieKiller opening balance: NPCs now spawn with separation, infections wait through a 3-second grace period, the configured starting horde stays at exactly 10, the arena uses stable lighting instead of entering a dark atmosphere phase, and its checker floor has a readable green tint.
- Added race-aware camera defaults. World 1 now faces down its first route leg, large legacy maps start farther/higher, and Piste Ovale gets a high chase view instead of placing the camera inside the banking.
- Corrected the Piste Ovale HUD to describe W/S throttle and A/D steering rather than generic camera-relative BallZ movement.
- Added live objective guidance to the Rings/Zombies HUD (`Collect nearest ring · Nm` / `Squash nearest zombie · Nm`) and to `render_game_to_text`.
- After-race ZombieKiller results now report zombies defeated instead of `0 / 0` rings.
- Changed panel reset from smooth to immediate scrolling so gameplay/results never inherit a partially scrolled race-selector panel.
- Verified the full progression loop after all nine completions: Next Challenge returned to Challenge 1, Tour Progress showed 9/9, Career Score showed 900, and every challenge card displayed its earned medal.
- Visually inspected gameplay captures for all nine challenges plus representative finish screens under `output/playwright/nine-race-matrix/`.
- Final verification passed: `npm run build`, the develop-web-game client `home -> tour -> gameplay -> forward/right/jump` smoke run, and the Playwright console check (0 errors, 0 warnings). The only build note remains Vite's advisory for large optional legacy-data chunks.
- 2026-07-16 correction audit started after user feedback: Impreza is visually front/back reversed and paints the body livery on the underside; BallZ lost its readable internal arrow; particles exist only as easy-to-miss white event bursts; low-resolution skybox faces are exposed too aggressively; the main UI still mixes BallZ races, recovered worlds, vehicles, and independent experiments; Math Lab controls are technically present but do not read as a real Math Game UI.
- Added `../ARCHIVE_REVIVAL_TRACKER.md` as the persistent source of truth. It separates project families, records exact `RESTORED/PARTIAL/REGRESSED/PIPELINE/MISSING/RESEARCH` states, includes the complete known archive/content/engine/editor/CubX/production backlog, and defines verification gates so proxy or hidden work is no longer called finished.
- Recovered the Impreza's original material slots from the 3DS face-material chunks instead of treating the chassis as one livery surface. Archive code confirms slot 0 = `ChassisSTI`, slot 1 = `Windows`, slot 2 = `UnderCarriage`; the converter now preserves those assignments and the web mesh uses the archived `ChassisSTi.bmp`, `Windows.bmp`, and `Undercarriage.bmp` separately.
- Restored the archived `Wheel.bmp` material and corrected the vehicle drive-force sign: W now propels toward the model's authored +Z nose instead of visibly driving the Subaru backwards.
- Restored BallZ controller UVs when swapping to the decoded 2011 mesh. `FireArrow800.jpg` now maps correctly again, the shell glass/wire treatment is cleaner, and the internal 3D direction arrow is visible rather than silently disabled.
- Replaced the particle system's tiny white square points with soft glow sprites, per-particle color, larger readable bursts, and explicit live particle state. Engine & FX Lab now runs three persistent orange/cyan/violet fountains on lit emitter pedestals; QA captured 332 active particles.
- Softened low-resolution cube-sky presentation with background blur/intensity tuning and anisotropy, and added honest UI disclosure of the source limits (Winter 256 px, most sets 512 px, LostValley 1024 px). This is mitigation, not a claim that new high-resolution source art exists.
- Split the main information architecture into BallZ Tour (6 BallZ/ZombieKiller races) and Recovered Worlds & Vehicles (World 1, Map 1, Piste Ovale). The latter explicitly labels current ring objectives as prototype recovery trials, lists Level0/1, slide/pipe/corridor, common rooms, and Dominus as the world queue, and tracks its progress separately.
- Fixed a lazy-load refresh bug found during the new Worlds-screen browser pass: `onRaceAssetsChanged` now refreshes both `race-select` and `world-select`, so World/car Start actions enable when their chunks arrive.
- Rebuilt Math Lab into a prominent Math Game workbench: large live equation, Parabola/Slope switch, five presets, A/B/C/M/X meanings, responsive labeled sliders, inactive-parameter indication, and live `render_game_to_text` formula state. The Wide Arch preset was verified end to end.
- Browser/game-client QA artifacts for this correction pass are under `output/web-game/revival-correction-*` and `output/playwright/revival-*.png`. Verified desktop/mobile home and Math UI, final Worlds menu, visible FX emitters, BallZ gameplay, Piste forward motion toward +Z, and the flipped-car undercarriage view showing the separate grey/mechanical slot.
- Final nine-playable regression matrix passed after the menu/material/control changes. Every entry loaded, accepted real ArrowUp movement, completed its live objective chain, and reached `after-race`: movement distances were 6.484m, 2.217m, 2.320m, 5.557m, 3.974m, 4.514m, 5.425m, 1.932m (Piste), and 5.557m; ring/zombie totals remained 16, 17, 16, 16, 12, 9, 10, 10, and 10.
- Final `npm run build` passes. Browser console verification reports 0 errors and 0 warnings. The only build note remains Vite's advisory about the intentionally lazy, large recovered legacy-data chunks.
- 2026-07-16 regression follow-up: reproduced BallZ leaking into Math/Scene Lab because the player shell lived outside the race-world group; `setRaceWorldVisible` now owns shell visibility too, including lazy rebuilds.
- Restored a persistent UI build badge (`v0.1.0`, `revival-2026.07.16-r2`) and mirrored it into `render_game_to_text` so screenshots and bug reports identify the running build.
- Reproduced the Impreza accepting `forward: 1` while remaining at zero velocity after the countdown. Cannon wheel/chassis bodies had fallen asleep and torque did not wake them; vehicle bodies now stay awake and are explicitly woken by throttle/steering. A delayed browser probe moved from `(28.96, 3.43, 5.00)` to `(-16.21, 3.56, 41.08)` and collected a live ring.
- Removed the skybox blur pass and moved to a wider camera-lens presentation. Initial sharp-sky visual QA confirms the smear is gone; FOV/oval surface treatment still being tuned in this follow-up.
- Static runtime texture-path audit found one real missing file, `Eroded scratch metal.jpg`; restored it from `Archive/bckup/BallZ2015.bckup/Media` and added explicit texture-load error logging/anisotropy.
- Final sky presentation removes blur entirely, uses moderate source-aware FOVs (Winter 76°, LostValley 64°, other sets 70°), lowers the Scene Lab camera toward the panorama horizon, and gives Home a stable ClearNight background instead of inheriting the last selected race sky.
- Fixed a second lazy-load boundary bug: late TVM/archive chunks rebuilt the hidden race world and silently replaced the Home/lab skybox. Late rebuilds now reapply the active preview sky and lens.
- Corrected generated box UV scale across the three archive arenas. Each box face now repeats its archived texture by world dimensions, so narrow walls and broad floors keep square, consistently sized arrow/checker/wood/grid tiles rather than stretching one UV square across every rectangle.
- Piste Ovale now uses face-projected tiling from the archived `Concrete.jpg`, avoiding both the false Damier checker and the TVM decoder's currently ungrouped/misaligned UV islands. Full TVM MATT/material-group parsing remains tracked work.
- Final delayed Impreza verification after 6 seconds idle: start `(28.91, 3.43, 5.00)`, then a 650 ms ArrowUp burst reached `(29.37, 3.43, 8.18)` at velocity `(1.77, 0, 9.67)` — 3.21 m of actual travel after the former sleep point.
- Final BallZ verification: a 500 ms ArrowUp burst collected the first ring and reached z=7.65 at 13.5 m/s; the shell is visible in gameplay and absent from Math/Scene screenshots.
- Final production verification: `npm run build` passes; static `/assets/...` source audit reports zero missing paths; Playwright production console reports 0 errors / 0 warnings across Home, Math, Scene Lab, BallZ and Piste. Final artifacts are `output/playwright/regression-*-final.png` and `output/web-game/regression-final-*`.
- Replaced the generated-box texture-clone tiling pass with per-face, world-scaled UVs. Rapid world switching had exposed 174 `Texture marked for update but no image data found` warnings while cloned textures were waiting for their shared image; the UV approach keeps proportional tiles without creating incomplete GPU uploads.
- Re-ran the complete nine-challenge production matrix in a fresh browser after the UV correction. Every world loaded, accepted ArrowUp movement, completed its real objective callbacks and reached `after-race`; Piste moved 1.813 m in the 450 ms Impreza probe.
- Final fresh-browser console result is 0 errors / 0 warnings across the complete matrix. The representative Piste capture shows stable face-projected concrete and Archive Level 1 shows populated, consistently scaled arrow surfaces.
- Final develop-web-game client rerun captured `output/web-game/regression-final-uv/shot-0.png` and matching `state-0.json`; the visible Tour preview has populated proportional face textures, the BallZ shell/arrow is present there, and the text state exposes `v0.1.0` / `revival-2026.07.16-r2`.
- 2026-07-16 archive-reference correction: the user supplied `screenShotLevel1.png`, `screenShotLevel2.png`, `screenShotLevel3.png`, and `Suzanne1.png` as the actual visual baselines and correctly rejected the current playable arenas as restorations. Copied those four files into `public/assets/references/` and show the selected image in the challenge spotlight with an explicit “archive reference target — not a screenshot of the current build” caption.
- Renamed the four selectors to `Study`/`Geometry Study` language and changed the persistent tracker statuses for Level 1/2/3 and Suzanne 1 to `REGRESSED`. Their exact layout, object population, lighting, camera and gameplay composition remain restoration work.
- First fidelity correction from the supplied screenshots: Level 1 uses the recovered archive `mud.bmp`, and the three classic arena studies use white spherical/orange-seam checkpoints instead of modern torus rings. This is closer to the visible archive vocabulary but does not satisfy the full reference match yet.
- Corrected project taxonomy again: World 1, Map 1, Level0/1 and slide/pipe/corridor/track work are BallZ concepts/world experiments; common rooms, terrain/sky scenes and Dominus are standalone 3D environments to discover; Piste/cars remain vehicle experiments. Home and the archive queue now say this explicitly, while dedicated selectors remain tracked work.
- Traced the missing CubZ behavior directly to `CubZ.cpp`: `RotateTo` plays `CubeRot.tva`, then `Open` plays `CubeOpen.tva`, enables one of eight menu levels with four internal buttons, and `Close`/`BackRotate` reverse the sequence.
- Replaced the decorative CubX proxy with an interactive procedural port of that state machine. Eight labeled cubelets are directly ray-pickable; the selected cube rotates, the body explodes/unfolds, four category-specific 3D panels appear, panel selection updates live state/highlight, and Close folds and returns the cube home. DOM mirror controls provide an accessible alternative.
- Added CubZ to the main home destinations, added live CubZ phase/selection/action data to `render_game_to_text`, and exposed QA hooks for select/action/close.
- Verification so far: `npm run build` passes; develop-web-game idle capture is `output/web-game/cubx-idle/shot-0.png`; Playwright verified mirrored selection, panel action, close/return, plus a direct canvas click on a 3D cube. `output/playwright/cubx-open.png` was visually inspected and the browser console reports 0 errors / 0 warnings.
- Remaining CubZ fidelity: decode the TVA rig/clips and the actual `CubXMesh.tvm`, `CubXBtn*.tvm`, `CubXBtn/Btn*.tvm` meshes, then replace procedural motion and labels without changing the now-working state flow.
- Final CubZ visual QA passed at desktop and 390x844 mobile sizes. `output/playwright/cubx-open-final.png` and `output/playwright/cubx-open-mobile-final.png` show the opened four-panel state with the version badge and controls remaining readable; the final browser console remained at 0 errors / 0 warnings.
- 2026-07-16 full scene-census pass: the external StockRoom contains the authoritative `levelList.xml` plus `Level1_base.ASCII`, `Level2_base.ASCII`, and `Level3_base.ASCII`. These establish exact 20×20 authored tile/checkpoint/start/half/finish layouts and the original ClearBlue/LostValley/NightSky assignments.
- Replaced the three hand-invented classic arena layouts, checkpoint routes, and unsupported moving hazards with a deterministic ASCII-level builder. T/Z/M create the authored solid tiles, R/r create white checkpoints, @ sets spawn, F/f and H/h create finish/halfway posts, and `$` preserves the Level 3 archive prop. Screenshot-level material, lighting, camera, and physics tuning remain intentionally partial.
- Completed a de-duplicated archive scene census across Scene3D, ArduinoGUI, legacy `3DScenes`, CubX/Atmel modes, StockRoom ASCII/TVM/XML content, common environments, and the standalone HLSL demo. The evidence ledger is `../ARCHIVE_SCENE_CENSUS.md`.
- Added a first-class **Complete Scene Index** home destination and archive mode. Its 52 records are grouped by project family/type, show source evidence and a direct revival check, and summarize 3 RESTORED / 20 PARTIAL / 1 REGRESSED / 17 PIPELINE / 11 MISSING / 0 RESEARCH.
- The census explicitly identifies previously untracked scene implementations: the five-cube Skybox Selector, Car Selector, FlightX pipe flight, Notes Manager, CubX Screensaver, Default/BallZ/Car scene classes, separate Arduino Intro/Math/BallZ screens, Suzanne 2, MyWorld/TestWorld/ObjectLibrary/BaseScene/test1 documents, Anneaux, Voie Lactée, Common Rooms, Dominus, and HLSL Shadow Mapping #23.
- Corrected the checkpoint audit after live text-state verification: each authoritative `Level1_base.ASCII`, `Level2_base.ASCII`, and `Level3_base.ASCII` file contains exactly **20** `R/r` checkpoints. Earlier 16/17/16 counts described the replaced prototype layouts, not the archive maps.
- Removed the stale Level 3 “Moving Hazards” chapter label because the exact recovered ASCII restoration intentionally removed unsupported invented hazards; it is now “Platform Clusters.”
- Desktop and 390×844 mobile visual QA passed for the new Scene Index summary and scrolled scene cards. Production console result: 0 errors / 0 warnings. Screenshots: `output/playwright/archive-scene-index-*-final.png`.
- Browser text-state verified Level 2 and Level 3 at `0/20` with their exact authored checkpoint coordinates. Visual comparisons are `output/playwright/ascii-level2-preview-final.png` and `ascii-level3-preview-final.png`; Level 3 is structurally close, while Level 2 still needs period gold-arrow material/height/camera tuning.
- The develop-web-game client started and exercised Archive Level 1 with a live `0/20` objective state, nonzero movement, no captured errors, and a populated exact ASCII scene. Artifacts: `output/web-game/ascii-level1-final/`.
- Current high-value restoration order: finish Levels 1–3 visual/physics fidelity, rebuild Suzanne 1 from its ASCII/XML/reference, restore the original Skybox and Car selectors, make FlightX/pipe1 playable, expose slide/track concepts separately, then assemble Common Rooms and ObjectLibrary/Dominus as discovery environments.
- 2026-07-16 reconciliation audit found that recovery code had advanced without matching updates to the census, in-app Scene Index, or this progress log.
- Confirmed `Archive/bckup/BallZ2015.bckup/Media/SlideLarge.TVM` and `Yanik C++ BCKUP/BallZ 2011/Release/Media/Level0.TVM` are byte-identical (SHA-256 `391591EFE67F03C13E6D0C56FAA3E1D0EF5B71C51CB74D94C4907EF1AD685546`). The new Great Slide mode therefore revives the existing Level0 census record rather than creating a 53rd scene.
- Production browser QA verified The Great Slide loads its lazy `slide-level` chunk, exposes recovered geometry, starts at `0/8`, accepts a sustained ArrowUp input, and moves with nonzero velocity. It remains partial because materials, original rules/checkpoint intent, camera, and complete finish-loop behavior have not been matched.
- Production browser QA verified FlightX loads decoded `pipe1` and the archive airplane and runs with 0 console errors/warnings. It remains partial and currently incoherent: gameplay starts at `0/0` rings with halfway already true, the HUD gives generic BallZ movement help, and the challenge metadata falls back to `Custom Challenge`.
- Static archive comparison found a FlightX control regression. The original `FlightXScene.cpp` uses Left/Right arrows for roll, Up/Down for pitch, W/S for relative thrust, Space as pitch-down airbrake, and R to reset. The current web flight maps the shared W/S-or-arrow forward axis to pitch and uses Shift for boost even though its subtitle promises W/S throttle.
- Verified the CubX screensaver behavior live: after menu inactivity, text-state camera yaw advanced from `0` to `0.752`; code uses the archived 10-second trigger and 5°/s orbit. Original `CubXScreensaver.tva`, aim/radius composition, and settings UI remain unrevived.
- Recovered systems present in code but previously untracked: classic-2015 BallZ preset (`B` toggle/localStorage), best-run ghost recording/shell/last-lap spline, EffectComposer `post_haze.shade`-style distortion, and animated water using the recovered distortion texture. Each is now tracked as partial because visible UI, exact fidelity, or complete QA is still missing.
- Corrected the 52-scene status totals to 3 RESTORED / 23 PARTIAL / 1 REGRESSED / 17 PIPELINE / 8 MISSING and synchronized `ARCHIVE_SCENE_CENSUS.md`, `ARCHIVE_REVIVAL_TRACKER.md`, and the in-app `ARCHIVE_SCENES` registry.
- Remaining forgotten UI/status debt: Worlds queue copy still treats Level0/slide/pipe as wholly queued, Great Slide and FlightX lack authored challenge metadata, FlightX lacks dedicated help/state/objectives, ghost/preset features are invisible, Scene Lab copy still calls the converted airplane queued in that surface, and the visible build label has not advanced beyond `revival-2026.07.16-r3`.
- Verification for this audit: `npm run build` passes; develop-web-game home smoke capture produced text state and no errors; Playwright visually inspected Home, Great Slide preview/gameplay, FlightX preview/gameplay, and Scene Lab; the production console reported 0 errors and 0 warnings.
- 2026-07-16 standalone-environment source audit chose `common/room2.tvm` / HLSL Shadow Mapping #23 as the fastest authentic discovery scene: its 105 vertices, 56 triangles, single bump-mapped material, transforms, teapot, light marker, point light, orbit camera, and A/D controls are all directly evidenced by archived data/code.
- Added conversion-only `tools/convert-common-scenes.mjs` and generated `src/legacy/common-scenes.json`; all three common TVMs decode positions, normals, UVs, indices, material assignments, and groups without fallback. No runtime integration was changed by this audit.
- Dominus fidelity warning: `ObjectLibrary.xml` is a 61-object editor/library grid rather than evidence for a composed village; the current village converter drops secondary materials on 23 multi-texture X files, cannot parse binary `port_crateshed.X`, and emits corrupt `woman.3ds` bounds. Any curated town/ring layout must remain labeled as a reconstruction/proxy, not an authentic recovered scene.
- Added the isolated `src/common-room-environment.ts` integration module. It creates an indexed Room 2 group with archived transforms, DDS diffuse/normal maps and repeat wrapping, the scale-3 teapot, low-poly light marker, non-attenuating point light with a 1024 cube shadow, archived orbit camera behavior/state, and complete disposal without changing shared menu/race files.
- Copied the two unmodified Room 2 textures to `public/assets/textures/common/tv3dlogo_{d,n}.dds`; their SHA-256 hashes remain byte-identical to `common/`. `npm run build` passes with the isolated module included by TypeScript.
- 2026-07-16 FlightX core restoration corrected the archive input split from `AtmelCubx/FlightXScene.cpp`: Left/Right arrows roll, Up/Down arrows pitch, W/S provide forward/reverse relative thrust, Space strengthens pitch-down, and R resets orientation. A/D no longer rotate the airplane through BallZ's shared movement axis, and Shift is no longer FlightX throttle.
- FlightX now starts stationary instead of crossing the halfway gate during the countdown and has a 14-ring recovery route around decoded `pipe1`; deterministic objective QA completed all 14 rings, the halfway gate, and the finish transition.
- `RaceDebugSnapshot` now exposes FlightX speed/input/orientation/forward/objective state plus the previously hidden BallZ preset and ghost state. The UI/text-state owner integrated these fields into FlightX-specific HUD and `render_game_to_text` during the same pass.
- FlightX QA passed: production build, required develop-web-game client capture, visual gameplay inspection, W forward collection (first ring), S reverse movement, isolated arrow rotation, stronger Space+Down pitch, R identity reset, A/D no-op for flight orientation, 14/14 deterministic completion, and 0 browser errors/warnings.
- r4 UI coherence pass supersedes the earlier reconciliation findings: Great Slide and FlightX now have authored challenge metadata and Ride/Fly verbs; FlightX has dedicated 14-gate HUD/help/live state; Worlds distinguishes playable partial recoveries from the remaining queues; BallZ preset/ghost status is visible; Scene Lab no longer calls the dedicated FlightX airplane merely queued; the badge/text state report `revival-2026.07.16-r4`.
- Integrated Room 2 as first-class `common-room-lab`, reachable directly from Home and Project Modes. It is not a `RaceDefinition`: the mode hides the BallZ/player/rings/gates/timer/scoreboard and exposes only the standalone room, teapot, light marker, point-shadow rig, orbit controls, and environment state.
- Room 2 production browser QA passed. The decoded room and archived DDS material render against the black demo background; Home → Room 2 loads `ready`, text state exposes the exact source hash/evidence/objects/light/camera, D changed orbit from 0.01° to 338.82° and camera x from 0.009 to -18.067, Esc returned Home, and the console remained at 0 errors / 0 warnings. Screenshot: `output/playwright/r4-room2-integrated.png`.
- Integrated FlightX browser QA independently confirmed an opening `0/14`, stationary speed 0, halfway false, W travel from z=8 to z=-4.31 plus first-gate collection, arrow roll quaternion change, R identity reset without leaving gameplay, and 0 console warnings/errors. Screenshot: `output/playwright/r4-flight-gameplay-integrated.png`.
- The BallZ HUD/text state visibly reported `Classic 2015` after B toggle and a live first-ghost recording sample count. Screenshot: `output/playwright/r4-ballz-preset-ghost.png`.
- Full visible-roster regression found and fixed a Suzanne-only QA regression: its deterministic completion hook simulated one halfway/finish pass even though the study is configured for three laps. The hook now runs the live callbacks once per configured lap.
- Kept the concurrent `dominus-port` prototype out of player-facing selectors/debug race coverage. Archive evidence shows its curated layout and BallZ ring framing are a reconstruction, not an authored Dominus village; `ObjectLibrary.xml` is an editor/catalog grid and the current converter loses many secondary materials.
- Final census after the Room 2/HLSL pass remains 52 distinct records: 3 RESTORED / 25 PARTIAL / 1 REGRESSED / 17 PIPELINE / 6 MISSING. Both Common Room 2 and the HLSL Shadow Mapping #23 demo move from MISSING to PARTIAL because the dedicated mode meaningfully reconstructs the shared archived composition.
- 2026-07-16 SBQC integration pass started from the user request to bring the public `WindriderQc/SBQC` visual experiments into GraphysX. Repository census found the earlier browser `archive/3D Playground/GraphysX` scene-manager lineage plus 60 Nature of Code application/source blobs covering walkers, forces, attraction, particles, steering/flocking, flow fields, recursive trees, falling leaves, DNA, reproduction, and ecosystems.
- Chosen implementation boundary: add an isolated deterministic `NatureLab` runtime rather than embedding old p5 pages or adding more hard-coded content to `RaceScene`. First live studies are Flock Planet, Forces & Flow Garden, and Living Forest; all expose serializable parameters and text/debug state for future agent tools. Licensing is deliberately deferred per user direction, while source paths remain recorded.
- 2026-07-16 classic ASCII fidelity evidence pass: `Level1_base.ASCII` is 20x20 with 106 `T`, 20 uppercase `R`, one `@`, and one each `F/f/H/h`; Level 2 is 20x20 with 140 `Z`, 20 uppercase `R`, one `@`, and one each `F/f/H/h`; Level 3 is 20x19 with 157 `M`, 20 lowercase `r`, one `$`, no `@`, and one each `F/f/H/h`. The objective count remains exactly 20 in every level; screenshot occlusion and older QA captures must not be used to reduce it.
- Added `src/classic-level-style.ts` as isolated renderer evidence plus exact copied StockRoom assets under `public/assets/textures/classic/`: Alien01 diffuse/normal, Checkerboard, Alien02 diffuse/normal, and Wood03 diffuse/normal. The style records the original ClearBlue/LostValley/NightSky choices, exact/source-backed tile surfaces where available, individual red/blue posts (no spanning arch in the references), camera framing, and scene-specific light balance.
- Restored the authored one-world-unit ASCII grid/default cube scale instead of the previous 1.42x enlargement; solid tiles are contiguous 1x1x1 cubes, floor edges are thin, checkpoints use the archived ground/elevated center heights, and posts use the archived 0.2x2 scale. Level 3's `$` is now correctly a solid two-way spawn tile at its exact ASCII x/z, not a prop or invented fallback.
- Restored `levelList.xml` progression facts for all three scenes: three laps, 10-second ring bonus from the archived gameplay mode note, exact `ScoreBest` values (29,815.1465 / 122,428.984 / 158,507.313 ms), and realistic targets (45 / 150 / 190 seconds). Level 2's archived human count is recorded as 10 without misusing the zombie objective system.
- Classic pass final QA: `npm run build` passes; the required game client reports Level 1 at its exact spawn with 20 checkpoints and a 3-lap objective; final browser captures `output/playwright/classic-fidelity-final-{green-grid-run,rotator-cube-works,piston-gateworks}.png` were visually inspected after renderer integration. All three accept real forward input, preserve 20/20 checkpoints, complete three laps into `after-race`, and report 0 console errors / 0 warnings. Exact floors/normals/tile surfaces, local lighting/camera, individual posts without arches, and Level 2's 10 non-hazard human proxies are live. Remaining uncertainty is limited to the missing archived Metal03 normal source for Level 3, the exact original human actor appearance, and screenshot-derived rather than serialized camera/light values.
- A deeper authoritative copy at `E:/Media/Datalake/Tech/Repos/GraphysX/GraphysX/SceneEditor.cpp::BuildASCIIScene` supersedes the incomplete in-workspace builder revision: it handles all `T/Z/M/r/$` symbols. `T` uses `twoway.jpg`; `Z` uses exact Wood03 diffuse/normal; `M/r/$` fall through to the same two-way diffuse treatment, with `r` also adding an elevated ring and `$` setting the player spawn. This proves Level 3's 20 `r` cells and one `$` cell are solid platform tiles, not holes or decorative props. The web layout now restores all 21 cells, places the 20 elevated checkpoints at the archived 1.75-unit center height, and uses `$` x/z as the exact spawn (with web-safe vertical clearance).
- Copied exact `Wood03_diff.bmp` and `Wood03_normal.bmp` into the isolated classic assets. Level 1 and Level 3 style records now use two-way on all cube faces; their dark edges come from scene lighting rather than an invented `xboxes03` side material. Level 2 uses Wood03 on all faces. The renderer still needs to consume these style records.
- 2026-07-16 r5 continuation added `window.advanceTime(ms)` backed by fixed 60 Hz RaceScene updates and an explicit render pass. The required game client now advances menu/game state deterministically; build and a clean rerun passed, with the first transient preview connection reset absent on retry.
- 2026-07-16 selector restoration audit recovered both selector implementations from `Yanik C++ BCKUP/CubXSolution`. `CLSkyboxSelectScene` is complete and exact: five named 50-unit cube icons (`ClearBlue`, `SkyX`, `ClearNight`, `LostValley`, `Winter`) sit on a radius-125 ring, rotate at 50°/s, start under a 45° camera at `(150,200,500)`, and use click -> stopped rotation -> camera chase/collision -> selected day panorama -> radius-500 panorama orbit semantics.
- Added the isolated `src/archive-selector-environments.ts` runtime module without changing `prototype-app.ts` or `race-scene.ts`. `SkyboxSelectorEnvironment` owns an activation-safe Three group/background/camera state, exact face textures and ring positions, ray-picked cube selection, the recovered zoom/panorama transition, state snapshots, reset/deactivation, and disposal. Public metadata exports preserve all five names, dimensions, transforms, camera values, and evidence paths for the main UI integrator.
- The same audit found that `CLCarSelectScene` is a real but unfinished selector, not a multi-car chooser: `QTY_CARS` is 1, the sole name is `Impreza`, its click callback is empty, and no code ever commits a car selection. The exact recovered composition is the 100×100 `height.jpg`/`ground.jpg` landscape at `(-50,0,-50)`, water y=2 using `distortiontexture.dds`, Impreza spawn `(30,100,0)`, and a 45° camera at `(75,100,50)` looking at the origin.
- `CarSelectorEnvironment` restores that evidence-bounded preview with the decoded Impreza chassis/four wheels, archived ChassisSTI/Windows/UnderCarriage/Wheel materials, exact terrain/water source assets, gravity from the archived Area physics settings, the original free-camera formula, state snapshots, and explicit `selectionImplementedInArchive: false` rather than inventing missing chooser behavior. `inspectFromNdc` is deliberately labeled web-only inspection; `selectByIndex` returns false to match the empty C++ callback.
- Copied byte-identical archive sources to `public/assets/selectors/car/{height.jpg,ground.jpg,distortiontexture.dds}`. SHA-256 sources are `45F76437...` for height and `05DEAEC7...` for distortion; the ground file comes from the only recovered `Media/Models/Ground.jpg` copy.
- Selector QA passed `npm run build` and the required develop-web-game client. Visual inspection confirmed the full five-cube ring (`output/web-game/selector-sky-icons/`), a real pointer click completed ClearBlue's zoom-to-panorama state (`selector-sky-panorama/`, `activeId: clearblue`), and the Car preview loaded exact terrain/water/car assets then settled the Impreza from archive y=100 to terrain y=4.958 (`selector-car/`). All text states reported `loadStatus: ready` and no client console/page errors were captured.
- Remaining selector fidelity gaps are explicit: TV3D's undocumented height-amplitude conversion is estimated at 22 units; the water restores exact DDS motion/material but not the old reflection/refraction render surfaces; the sky zoom duration approximates collision timing because TV3D `ChaseCamera(...,2)` smoothing is engine-specific; and the Car Selector has no authentic selection/confirmation transition to recover because its archived `MeshClickedAction` body is empty.
- Integration hardening keeps `cars-catalog.json` behind a dynamic import, matching the existing race scene's lazy legacy-data strategy instead of adding 276 KB to the initial bundle. The selector module passes an isolated strict TypeScript compile and the required game client again reported `loadStatus: ready` with the lazy catalog (`output/web-game/selector-car-lazy-final/` and `selector-car-lazy-load/`). Whole-app build reruns were temporarily blocked by concurrently edited Suzanne/Nature/UI integration files outside this module; `archive-selector-environments.ts` itself reported no TypeScript errors.
- 2026-07-16 Suzanne forensics corrected a major archive conflation: the current `Suzanne1.blend.x` circular machinery study is a separate 2015 branch, while `Suzanne1.png` depicts the later authored `Suzanne1.ASCII` arena. The missing StockRoom source was recovered at `E:/Media/Datalake/Tech/StockRoom/Suzanne1.ASCII` (SHA-256 `64EC6746...`) along with its compact/backup XML and exact textures.
- Added deterministic `tools/convert-suzanne-ascii.mjs` and generated `src/legacy/suzanne1-ascii-scene.json`. Exact `BuildASCIIScene` decoding yields a 40x40 grid: 208 textured walls (`#` 168, `Z` 4, `z` 36), 45 `c` chain assemblies, 15 `R` Z-textured sphere pickups, 3 pistons (`0/6/9`), 2 `s` magician/dual-particle cells, 2 `C` CubX anchors, player `@` at `[5,0.5,36]`, finish endpoints `[10.5,0.5,34.5]`/`[1.5,0.5,34.5]`, and halfway endpoints `[0.5,0.5,32.5]`/`[14.5,0.5,9.5]`.
- The generated scene preserves exact later-source scales: walls 1x1x1; chain base 0.25 cube plus two 0.25-high cone links with radii `0.25/1.75` and `0.25/1.5`; lap posts radius 0.2/height 2; ring radius 0.5; piston bar `[5,0.1,0.1]`, plate `[0.25,0.95,0.95]`, trigger `[0.75,0.95,0.75]`, and linear limits `[-0.5,0.5]`.
- Added isolated `src/suzanne1-ascii-environment.ts` plus `suzanne-ascii-preview.html`. It restores the grid, source rings, chains, lap markers, pistons, XML magician billboard, archived point lights/default directional light, ring visibility/reset, piston activation, deterministic ring rotation, state snapshots, and disposal without editing the main race/UI files. Exact StockRoom assets copied under `public/assets/textures/suzanne1/`: GrassSample, ZRing, objet39, 3D_Spheres, Podium, Zack, and particle DDS.
- Two honest presentation profiles prevent another false merge: `source2017` keeps exact concrete/objet39/radius-0.5/scale-1 bindings; `reference2016` uses screenshot-backed grass/wood, radius-1.5 pickups, and 3x chain presentation to match the visibly larger balls/block field. Those earlier size/material bindings remain labeled inference because the pre-2017 binding table is absent.
- Suzanne isolated QA: converter invariants pass; the module passed full `npm run build` twice before unrelated concurrent Nature/selector type errors appeared; the required game client reported `loadStatus: ready`, all exact counts, and no captured page errors in `output/web-game/suzanne-ascii-final/`. Both the reference profile and exact later-source profile (`output/web-game/suzanne-ascii-source2017/`) were visually inspected. Preview debug QA changed rings 15→14 and activated piston index 1 through the public hooks.
- Remaining Suzanne integration/fidelity: replace the current circular X study in the player-facing challenge with this environment/data; attach the existing BallZ controller at the authored start; use the exact half/finish segments and three-lap rule; wire ring/piston physics; restore the two CubX actors and Airplane XML attachment; reproduce the legacy HUD. Exact pre-2017 camera, texture binding table, random-human seed/count, and airplane interaction remain unknown.
- 2026-07-16 Nature Lab implementation now makes the SBQC reflection concrete. Added isolated `src/nature-lab.ts` with three deterministic 3D studies: Flock Planet (132 spherical boids plus planet/atmosphere/stars/trails), Forces & Flow Garden (176 mass particles, 18 walkers, inverse-square attraction, visible flow field), and Living Forest (13 recursive trees, instanced DNA leaves, seasonal fall, mutation, generations).
- Integrated first-class `nature-lab` navigation, Home destination, source-aware recovery board, responsive workbench, live sliders, study switching, reset/trail controls, and dedicated camera/sky presentation. `window.__GRAPHYSX_DEBUG__` now exposes Nature Lab open/select/set/toggle/reset operations and `render_game_to_text()` reports its seed, population, generation, settings, elapsed time, and visible systems while hiding irrelevant BallZ state.
- 2026-07-16 r5 live integration: the three StockRoom ASCII races now bind their recovered skies, floors and tile maps, exact four-post gates, three-lap rules, archived best scores, and Level 2's ten deterministic human proxies. Level 3's `$` is corrected as its spawn cell. Generic oversized floors and spanning arches no longer overwrite these authored compositions.
- Replaced the player-facing Suzanne circular-machinery substitution with the recovered 40×40 `Suzanne1.ASCII` arena shown in `Suzanne1.png`. The live challenge owns 208 wall cells, 45 chain assemblies, 15 sphere checkpoints, three animated pistons with collision bodies, two effect cells, exact start/finish/diagonal-halfway segments, and three-lap completion/bonus flow. The circular X meshes remain honestly catalogued as a separate moving-parts study.
- Integrated dedicated Skybox Selector and Car Selector modes into Home and the archive-mode navigation. The former exposes all five archived sky cubes, continuous rotation, click/zoom/panorama and reset; the latter exposes the one-Impreza height-terrain/water/free-camera preview without inventing a selection result for the archive's empty click callback.
- Reconciled the 52-scene census, tracker and in-app registry to 3 RESTORED / 28 PARTIAL / 0 REGRESSED / 17 PIPELINE / 4 MISSING. The remaining Level 1–3/Suzanne camera, lighting, physics, actor, HUD and uncertain-binding gaps remain named instead of being hidden by a false restored status.
- Selector visual QA caught inherited Home fog obscuring the sky cubes and potentially the car terrain. Both dedicated selector previews now clear host fog/environment state, and sky icon materials explicitly opt out of fog so their recovered face textures remain readable against the archived black background.
- Added a permanent `npm run qa:r5` browser matrix covering the Skybox Selector panorama transition, Car Selector terrain/Impreza/archive-empty-selection contract, Nature Lab isolation, all three exact classic races, and the corrected Suzanne 40×40 counts plus live three-lap finish callbacks. It captures full UI screenshots, text state, assertions, and console/page errors under `output/playwright/r5-restoration-final/`.
- Full-page screenshot inspection found the Car Selector's source-era camera bug still leaking through: the archived routine resets its local angles on every call (the C++ itself comments that the angle is never modified), so idle updates snapped the view horizontally at y=100 and hid the settled car. The port now preserves the current view ray across free-camera updates, refreshes UI state when gravity settles, and QA requires both car y<10 and a downward look target.
- Classic-level screenshot inspection also caught a generic 14–22 unit decorative floor halo surrounding Levels 1–3. It was not present in any supplied archive reference and visibly obscured the small authored boards, so classic styles now suppress it just as the recovered Suzanne scene does.
- BallZ scale is corrected for the authored ASCII family. `ArduinoGUI/BallZScreen.cs` creates its sphere at radius 0.3 and the recovered Suzanne data independently records visual radius 0.3/internal radius 0.225; the revival's 0.88 sphere was visibly oversized and physically unable to honor source-unit corridors. Levels 1–3 and Suzanne now scale both rendered shell/controller and Cannon collision sphere to 0.3, use matching floor/top-tile spawn heights, expose the radius in text/debug state, and use a radius-aware checkpoint pickup distance.
- Final r5 QA after the scale/camera/halo fixes: clean production build; `npm run qa:r5` passed 39/39 assertions with 0 console/page errors across selectors, Nature, Levels 1–3 and Suzanne; all seven full-page captures were visually inspected. The required game client additionally drove Level 1 forward/right/jump with the live 0.3 collision sphere from authored start to x=10.13/z=7.63, reported three laps and 20 checkpoints, preserved the internal arrow/rolling shell, and produced no error file in `output/web-game/r5-archive-ball-movement-final/`.
- Main-menu family coherence pass: removed the stale “Suzanne machinery” label and split the old combined Archive Concepts/Flight/Worlds destination into direct BallZ Concepts & Slides, FlightX, and Vehicle Experiments cards. The shared browser now renders three named, independent selectors and progress summaries; standalone environments remain outside that challenge browser. Next/replay progression stays inside the active family instead of silently jumping from a BallZ concept into FlightX or the Impreza.
- Integrated Common Room 1 and `sky.tvm` into the dedicated Standalone 3D Environments menu beside Room 2 Shadow Lab. Room 1 exposes its exact 180-vertex/250-triangle inward shell and archived logo DDS pair; `sky.tvm` exposes its exact 726-vertex/1,200-triangle six-group skydome diagnostic. The UI explicitly records that neither has a recovered authored host assembly and does not count the skydome component as another scene.
- Added `SBQC_INTEGRATION_CENSUS.md` at the workspace root to record the live source mappings, the 60-file Nature of Code census, earlier GraphysX/Globe/Trajectory/Starfield lineages, reusable 2D surfaces, and the next Orbital Observatory/world-recipe queue. Production `npm run build` passes; browser/world QA remains the next step.
- Nature Lab final QA is complete. The immutable production build was visually inspected in all three studies (`output/playwright/nature-lab-production-{flock,forces,forest}.png`); the required develop-web-game client capture is `output/web-game/nature-lab-release/`. Structured state reported 132 flock agents, 194 force-garden bodies/walkers, and 1,497 forest leaves; UI/agent tuning updated values live, trails toggled off, an 8-second forest cycle advanced generation 1→2, reset restored generation 1/elapsed 0, and all production runs captured 0 console warnings/errors. Final `npm run build` passes.
- 2026-07-16 Orbital Observatory pass audited SBQC commit `42c210f...` directly. Recovered `Globe`, `Trajectory`, `Starfield`, `earth3D`, and `threeEarth` behavior plus exact day/night/mask/cloud Earth maps, the transparent ISS cutout, and the 8,698-row quake source. Added deterministic `tools/convert-sbqc-quakes.mjs`, which keeps the top 160 finite events (M4.9–M7.1) with source commit/path provenance in `src/legacy/sbqc-quakes.json`.
- Added the versioned `graphysx.world/v1` contract in `src/world-recipes.ts`. Built-in recipes now describe all four studies with seed, named settings, visible layers, and optional observer. Nature Lab can export/load recipes, set layers, and relocate an observer; `window.__GRAPHYSX_WORLDS__` is the stable agent-facing list/current/load/select/setParameter/setLayer/setObserver/reset surface.
- Added isolated `src/orbital-observatory.ts` and integrated it as Nature Lab World 4: textured day/night Earth, independent cloud/atmosphere layers, geographic reference grid, 160 instanced quake spikes, source ISS sprite, inclined trajectory/samples, Québec observer radius, deterministic ground position, distance, entry/exit prediction, and live telemetry. First `npm run build` passes; browser visual/control QA is next.
- 2026-07-16 skybox-orientation correction started after user reported every set had misplaced/rotated faces. Pixel-edge comparison across ClearBlue, SkyX, Winter, and LostValley proved the shared TV3D layout: Three's +X/-X inputs need archived left/right (not right/left), archived up needs a 90° clockwise turn, and archived down needs a 90° counter-clockwise turn. Added one shared loader/conversion path for race/lab panoramas and selector cube icons so the mapping cannot diverge between modes.
- Added `npm run qa:skyboxes`, which deterministically selects all five archive skies and captures every face center plus every vertical seam. The first run rendered all 20 cardinal views successfully; its only console output was Chromium's expected screenshot `ReadPixels` performance warning, now filtered from application errors for the seam-focused rerun.
- Skybox correction final QA: the production server captured 40 views (five sets × four face centers × four centered vertical joins) under `output/playwright/skybox-orientation/`; the contact sheet was visually inspected and all mountain/horizon joins are continuous. Quantitative source-edge checks put the corrected non-ClearNight joins at 0.7–10.5 average RGB levels across JPEG/BMP boundaries. `qa:skyboxes` reported 0 application console/page errors, and the full r5 production matrix passed 39/39 with 0 errors. A scoped strict TypeScript compile of `archive-skybox.ts`, `archive-selector-environments.ts`, and `race-scene.ts` passes. The whole-app build had passed immediately after the skybox implementation, but a later concurrent unrelated edit now blocks a fresh whole-app build at `prototype-app.ts(970,24): Cannot find name 'MathUtils'`; that separate work was intentionally left untouched.
- 2026-07-16 Common Room 1 / `sky.tvm` audit corrected another scene-classification ambiguity. Exact TVM evidence: `room.tvm` is a 25-unit inward-facing room shell (180 vertices / 250 triangles) with one embedded `tv3dlogo_d.dds` + `tv3dlogo_n.dds` material; `sky.tvm` is a unit-radius inward-facing subdivided skydome (726 vertices / 1,200 triangles) with four `mid_day.dds` side groups plus 32px `top_pole.dds` / `bottom_pole.dds` caps.
- Repository-wide source search found no archived code that loads either `room.tvm` or `sky.tvm`; only `room2.tvm` has a recovered authored assembly (`#23 HLSL_Shadow_Mapping/main.cpp`). Therefore `sky.tvm` is evidence-classified as an environment component rather than a distinct discoverable scene, and any Room 1 camera/light/transform remains an explicitly labeled restoration choice.
- Extended `tools/convert-common-scenes.mjs` with that exact/inferred assessment and deterministic export of all five byte-identical DDS dependencies to `public/assets/textures/common/`. Source/output SHA-256 hashes match for every file.
- Added isolated `src/common-archive-environment.ts` and `common-archive-preview.html`. Room 1 uses the exact 180-vertex shell, DDS diffuse/normal maps, and source-scale 25-unit enclosure; its camera/orbit/lights are deliberately labeled inference. The separate sky diagnostic uses the exact six-group inward skydome and byte-identical mid-day/pole DDS files, while state explicitly reports `not an evidenced standalone scene`.
- Common visual QA caught and corrected both DirectX texture-V handling and TV3D left-handed +Z conversion: the final Room 1 logos are upright and readable rather than upside-down/mirrored. Required game-client captures under `output/web-game/common-room1-handedness/` and `common-sky-handedness/` were visually inspected; both report `loadStatus: ready`, exact counts/hashes, responsive orbit state, and no captured error files.
- Added `tools/verify-common-archive.mjs`; it asserts Room/Sky geometry and material-group counts, all six sky assignments, the component classification, and byte equality of all five DDS exports. The verification script and full `npm run build` pass. Remaining unknowns are the authored Room 1 camera/light/transform and any original host scene that used the skydome; no such source survives in the searched repository.
- Integrated and visually verified the Common archive in the main app: Room 1, the sky.tvm component diagnostic, and Room 2 Shadow Lab now have direct selectors, live orbit controls, UI evidence boundaries, debug state, and full-page captures. Expanded r5 QA passes 60 assertions with zero console/page errors. The synchronized 52-scene census is now 3 RESTORED / 29 PARTIAL / 0 REGRESSED / 17 PIPELINE / 3 MISSING.
- Integrated BallZ 2011 Level1.TVM as a dedicated non-race concept visit with exact 828-vertex/1,648-triangle geometry, two closed components, reversible normalization, and optional diagnostic edges/bounds. It is explicitly separate from the later ASCII Level 1, and no material/gameplay is invented.
- Integrated the CubX Notes subsystem as its own CubX destination: exact 50-slot marble block, zero-note initial state, source positions, sequential add interaction, 3D add-cube picking, capacity and reset. UI and state explicitly say the missing NoteMgrGUI layout/text/edit/delete/save/load model is not recoverable yet.
- Integrated Voie Lactée as the implemented five-body planetary vignette—not a star field—with exact GraphysX 2017 assets/transforms and a separately labeled partial BallZ 2015 motion profile. QA verifies five bodies, zero generated stars, exact later binding, and recovered Moon orbit rate.
- Bumped the visible build to `revival-2026.07.16-r6`. Expanded main-app QA passes 82 assertions with zero console/page errors, verifies the visible build/census totals, and captures all four newly integrated archive destinations under `output/playwright/r6-restoration-final/`. The synchronized 52-scene census is now 3 RESTORED / 32 PARTIAL / 0 REGRESSED / 17 PIPELINE / 0 MISSING; feature-level missing work remains tracked separately and no PARTIAL scene was promoted to RESTORED without full fidelity.
- 2026-07-16 BallZ 2011 Level 1 forensics: the distinct `Yanik C++ BCKUP/BallZ 2011/Release/Media/Level1.TVM` is healthy and is not the StockRoom `Level1_base.ASCII` board. Deterministic `tools/convert-ballz2011-level1.mjs` decodes one `Line01` object with 828 vertices, 1,648 triangles, one material index, and two adjacent closed 414-vertex components. Its archive span is genuinely huge (210.160187 x 498.72876 x 1,135.443969 units); topology and coordinates disprove a corrupt-bounds explanation.
- The Level1 TVM has identical UVs on every vertex, no embedded texture name, and one all-zero material assignment. Therefore an exact texture binding cannot be claimed. The generated `src/legacy/ballz2011-level1.json` preserves exact normals/indices/topology and a reversible, explicitly inferred display transform: archive X/Z are centered, archive minimum Y becomes zero, and one uniform 0.1 scale preserves shape and orientation.
- 2026-07-16 Notes Manager audit found a complete but narrow 3D subsystem rather than a full note application: one 50-slot `CLBlocNote`, exact 5x10 marble-box placement, zero initial notes, and a 30-unit add cube at `(0,-250,0)` that enables one note per click. `NOTEMGR_MODE` is declared without a matching `CLArea::RenderArea` branch; the actual block renders inside `CUBX_MODE`. The referenced `GUI/NoteMgrGUI.layout` is absent, and no note text/edit/delete/save/load model exists.
- Added isolated `src/notes-manager-environment.ts` and `notes-manager-preview.html` with the byte-identical 640x480 `marble10.jpg`. The module restores exact mesh/material/add/capacity behavior while state and evidence explicitly mark the full GUI as unrecoverable without invention.
- Voie Lactée audit corrected the census description: no source generates a star field. Both complete branches build a five-mesh planetary vignette (Earth, cloud shell, Moon, Mars, Venus) as part of BallZ/editor scenes. The 2015 branch rotates Earth/clouds and orbits the Moon; the later `GraphysX_1::CreateVoieLactee` branch is static, changes spacing, and tilts Earth 23 degrees.
- Added isolated `src/milky-way-environment.ts` and `milky-way-preview.html` with exact later StockRoom maps. The exact later profile preserves its authored fully enclosed Moon; the explicitly partial 2015 profile preserves positions/motion while disclosing substituted maps, the missing `EarthCloudsMapHigh.png`, engine-specific blend/orbit semantics, and the source's overlapping Mars/Venus geometry.
- Permanent `scripts/qa-notes-milky-way.mjs` passed actual add-cube raycasting, first/second exact note positions, capacity 50, reset, exact later planet transforms, no invented stars, and deterministic 2015 rotation/orbit assertions. Required game-client and Playwright captures under `output/web-game/{notes-manager-audit,milky-way-2017-audit,milky-way-2015-audit}` and `output/playwright/notes-milky-way-audit/` were visually inspected with no captured console/page errors. Full `npm run build` passes; only the existing large optional-chunk advisory remains.
- Added isolated `src/ballz2011-level1-environment.ts` and `ballz2011-level1-preview.html`. The module owns the exact indexed TVM mesh, neutral inferred material, optional edge/bounds diagnostics, conversion helpers in both directions, source/normalization/material evidence, structured state, and disposal; it does not invent a spawn, checkpoint route, finish, camera provenance, texture binding, or game rule.
- Level1 diagnosis is now reproducible: each closed component has 414 vertices; both have identical Y/Z bounds and sit beside one another with a decoded 0.018006-unit X seam. The display conversion round-trips to archive coordinates with at most 0.000005 source-unit error, so the 1,135-unit Z span and 499-unit vertical span are authored mesh extent rather than a normalization mistake. Calling it a paired/two-lane course remains an explicitly labeled visual inference.
- BallZ 2011 Level1 isolated QA passed converter invariants and an isolated strict TypeScript compile. The required web-game client exercised deterministic time, auto-orbit, and the bounds toggle; `output/web-game/ballz2011-level1-final/shot-0.png` was visually inspected and its state reported exact 828/1,648 counts, two closed components, reversible normalization, and no error file. Whole-app `npm run build` was blocked by a concurrent unrelated `prototype-app.ts(970,24): Cannot find name 'MathUtils'` error; this task was forbidden from editing that integration file.
- BallZ 2011 Level1 integration TODO: place this module in the Worlds/Concepts menu as a distinct discovery/course study, then separately research a source-backed spawn and collision convention before making it playable. Keep the StockRoom ASCII Level 1 race separate and do not apply `yellowtwowaysign.jpg` merely because it is adjacent in the archive—the TVM itself supplies no usable UVs or texture filename.
- 2026-07-16 Orbital Observatory final QA is complete. The permanent production-browser matrix now covers recipe loading, the 160-event quake field, predicted observer pass entry, layer control through `window.__GRAPHYSX_WORLDS__`, canonical reset, and agent relocation into an active pass; it passed 48/48 assertions with 0 console/page errors. Production screenshots under `output/playwright/orbital-observatory-*.png` were visually inspected, the required web-game client stayed error-free, and the final `npm run build` passes with only Vite's existing optional-chunk size advisory.
- 2026-07-16 Nature Lab decompaction pass started from direct SBQC source review. The former three blended Nature-of-Code worlds are now organized as 12 focused lessons plus the independent orbital lesson: isolated separation/alignment/cohesion/full flock; two-walker random walk, attraction/mass, animated flow field, combined forces; recursive branching, leaf fall, DNA/evolution, complete life cycle. Each lesson exposes a source mapping, concept, observation prompt, effective rules, phase, pause/step/reset, and a source-inspired experiment action; world recipes can retain an optional lesson and the agent API gains selectLesson/pause/step/act. Runtime behavior is lesson-specific rather than merely relabeled; build and interaction QA are next.
- Nature gallery final QA is complete. Source behaviors restored include the HTML flock's 60 initial boids, click-to-add interaction, active trails for every boid with the source's 45-sample limit; Session 1's two independently colored walkers with long persistent paths; isolated fixed-attractor particle spawning; animated flow vectors with 18 followers; separate recursive-growth, autumn-fall, generation, and planting actions. The workbench now shows 13 lesson cards, source/concept/Observe/effective-rule/phase panels, live elapsed/phase telemetry, focused sliders, Experiment, pause, deterministic step, reset, and canvas-click action. `npm run build` passes; required game-client capture `output/web-game/nature-gallery-release/` is clean; `npm run qa:nature` passes 42/42 with 0 errors; the restarted full r6 matrix passes 82/82 with 0 errors. The first full-matrix attempt had one transient preview connection reset on Suzanne's large optional chunk and passed cleanly after restarting the immutable preview.
- 2026-07-17 slide/track-family audit added deterministic `tools/convert-ballz-slide-track-family.mjs` and `src/legacy/ballz-slide-track-family.json`. It verifies 32 surviving copies across 10 named assets/revisions: 3 active Atmel Slide1, 3 later StockRoom Slide1, 3 Slide1a, 2 Level.Slides, 2 Level.Steps, 3 SlideBump, 3 SlideBumpGridTex, 3 SlideLong1, 4 BallZTrack1, and 6 pipe1 copies. Every same-ID copy is byte-identical.
- Two census-level aliases are now proven. `SlideLong1.TVM` is byte-identical to the recovered BallZ 2011 `Level1.TVM` (SHA-256 `5748B62F...D5E61`) and must not create another scene. Every located `pipe1.tvm` is SHA-256 `36C0B46C...7284A`; searched source loads it only through FlightX at scale 5 / `(0,-500,0)`, so there is no evidence for a separate BallZ pipe scene.
- `Slide1.TVM` has two incompatible surviving revisions that must not be silently swapped. The source-loaded Atmel/CubX binary is SHA-256 `3180052A...5602`, 566 vertices / 552 triangles, spans `251.586 x 2049.000 x 3713.869`, has 12 material groups, usable UVs, and three embedded path names. The later StockRoom binary is SHA-256 `B1B5066C...839C`, only 282 vertices / 556 triangles, spans `8.804 x 2.017 x 12.444`, and has identical UVs with no located host loader.
- The active Atmel Slide1 is the strongest distinct next candidate because source preserves its whole assembly: slide scale 1 at `(0,-5000,0)`, exact `Ball.tvm` scale 0.5 / spawn `(-50,-4750,-250)` / mass 5000, chase offset `(0,350,300)`, gravity `-9.800908285`, material constants, and slide/ball friction. It is initially a non-race visit rather than a faithful playable claim: `PushBall` declares four directions, but only the backward impulse remains active while the other torque blocks are commented out; the 100 diagonal rings are explicitly labeled temporary “for fun” positions in source.
- Remaining exact audit facts: Slide1a is source-loaded in an older branch (2,271v/2,849t) but lacks the later complete spawn/physics evidence; Level.Slides and Level.Steps have no located loaders; SlideBump and SlideBumpGridTex share exact bounds/triangle count but are distinct vertex/UV revisions, with GridTex embedding `EarthGri.bmp`; BallZTrack1 has a static CLBallZScene host, but Update/Input are empty and its 100 rings are explicitly placeholders.
- 2026-07-17 Agent World API v2 implementation: added isolated `src/agent-world-runtime.ts` with a renderer-independent `graphysx.agent-world/v2` definition/state contract, 11 entity/light types, transforms/materials/hierarchy/tags, five deterministic behaviors, spatial/semantic queries, atomic transactions with rollback, undo, deterministic pause/step, export, and named persistent snapshots. Invalid create/load now restores the prior world atomically as well.
- Integrated the runtime into RaceScene as the visible Agent World Studio and into the main home/project navigation. The Studio renders an API-built constellation, live entity graph, recent event stream, capability list, example agent code, and visible demo/pause/step/undo/save/clear controls. `render_game_to_text()` now reports `agentWorld`; `window.__GRAPHYSX__` is the dedicated v2 public agent surface while the focused Nature `window.__GRAPHYSX_WORLDS__` v1 API remains intact.
- Added `AGENT_WORLD_API.md` and updated README/SBQC census direction. `npx tsc --noEmit` and the first full production build pass. Remaining this pass: permanent API-only Playwright QA, required web-game client run, visual inspection, and full r6 regression.
- Added isolated `src/ballz-slide1-environment.ts` and `ballz-slide1-preview.html`. The environment uses exact selected Slide1 and Ball.tvm indexed geometry, exact source transforms, the source-wide untextured `StdMat` override, Ball mass/spawn, gravity/friction/camera evidence, reversible display conversion, source-chase and overview camera profiles, optional Ball/edge/bounds diagnostics, structured state, and disposal. It intentionally exposes `playable:false` and does not render the explicitly temporary 100-ring diagonal.
- Visual inspection confirmed both intended inspection paths. `output/web-game/ballz-slide1-initial/shot-0.png` shows the full long chute and exact Ball spawn in the inferred overview; `output/web-game/ballz-slide1-source-chase/shot-0.png` uses the uniformly translated exact chase offset and clearly frames Ball.tvm over the starting chute. `output/web-game/ballz-slide1-toggle-qa-rerun/` verifies the Ball visibility and bounds toggles through the required game client. All final runs have matching text state and no error file; one earlier toggle attempt hit a transient Vite HMR connection reset and passed immediately on clean retry.
- Added `tools/verify-ballz-slide-track-family.mjs`; it asserts the 10 records, 32 byte-identical same-ID copies, both Slide1 revision hashes/counts, SlideLong1 and pipe aliases, Bump/GridTex remap facts, exact selected transforms/camera disclosure, placeholder-ring/incomplete-control boundaries, and reversible normalization. The verifier passes, isolated strict TypeScript passes, and the final whole-app `npm run build` passes with only Vite's existing optional-chunk size advisory.
- Integration API: `BallzSlide1Environment` exposes `group`, `slide`, `ball`, `staticData`, `sourceWorldToDisplay`, `displayToSourceWorld`, `getCameraProfile('overview'|'source-chase')`, Ball/edge/bounds visibility setters, `getState`, and `dispose`; `BALLZ_SLIDE_TRACK_FAMILY_EVIDENCE` exposes the complete audit and status recommendations without loading every family mesh into the live app.
- 2026-07-17 XML document audit added reusable `tools/convert-xml-scenes.mjs`, which normalizes both archived serializer generations (`CScene3D/Object3D` and `Scene3D/Obj3D`) without assuming that their numeric action enums match. Generated `src/legacy/xml-scene-documents.json` records exact source hashes, object/action fields, resolution status, and evidence-bound classification for MyWorld, MyWorld - Copie, TestWorld, ObjectLibrary, BaseScene, and test1.
- The audit prevents false world claims: ObjectLibrary is 61 intentionally gridded catalog entries (not an authored village); BaseScene is 18 identical default cubes overlapping at the origin; test1 is a one-cube schema smoke document; MyWorld is a broken editor save whose two type-14 DUPLICATE entries incorrectly contain file paths; TestWorld is loader-coverage data with a missing bush and two invalid empty duplicate targets.
- `MyWorld - Copie.xml` is the smallest nontrivial authored document with strong recovery coverage: four exact procedural physics primitives, exact AirplaneLP.TVM and Level2.TVM, and one absent ArcheChinois.TVM (6/7 renderable objects). The converter generated `src/legacy/xml-myworld-copy-scene.json`, preserving Airplane's 5,367 vertices / 9,202 triangles / eight material groups and Level2's 448 vertices / 692 triangles / two groups, and exported eight byte-identical referenced textures under `public/assets/xml-scenes/`.
- Added isolated `src/xml-scene-environment.ts` and `xml-scene-preview.html`. The environment converts source +Z to web -Z, preserves all seven serialized transforms and physics metadata, renders the four exact procedural primitive definitions plus both decoded TVMs, and leaves the missing ArcheChinois absent rather than substituting a proxy. Camera and lights are explicitly disclosed inspection choices; physics remains unsimulated because no host gravity/timestep/ground record survives.
- `tools/verify-xml-scenes.mjs` passes deterministic coverage for all six documents / 99 objects, selected-scene transforms and resolution counts, exact TVM geometry/material counts, source TVM hashes, and all eight byte-identical texture copies. Browser QA under `output/web-game/xml-myworld-copy-close/` visually confirmed the exact textured primitives and airplane, deterministic orbit, 6/7 state, and missing-arch disclosure with no captured error file; an additional headless interaction check passed Airplane hide/reset, 0.75-radian orbit, and zero console/page errors.
- XML census recommendation: only `MyWorld — Copy` has earned `PARTIAL` (six of seven objects visibly recovered). `MyWorld`, `TestWorld`, `ObjectLibrary Environment`, `BaseScene`, and `test1` remain `PIPELINE`: they are audited/parsible but not revived, and ObjectLibrary must no longer be described as an authored tree/camp/port world. The separate Dominus Village/Port record cannot use ObjectLibrary's catalog grid as authored layout evidence.
- Whole-app build after the XML work is currently blocked only by a concurrent BallZ Slide1 integration error at `race-scene.ts(4488)` (TS2367: its narrowed preview-mode union excludes `ballz-slide1`); the XML files pass an isolated TypeScript compile and this task did not edit shared integration files.
- 2026-07-17 CubZ fidelity pass replaced the generic cube cluster with the exact eight `CubZ.cpp` interaction centers: `(0,25,0)`, `(0,25,100)`, `(-95,25,100)`, `(-95,25,0)`, `(0,-70,0)`, `(0,-70,100)`, `(-95,-70,100)`, and `(-95,-70,0)`. The live state now exposes source `fSize=8`, the 58-unit pre-scale hit-box dimension, one uniform reversible browser presentation scale, exact source/display centers, and an explicit `CubeRot.tva` / `CubeOpen.tva` fidelity boundary.
- CubZ full-chain QA now verifies closed source layout, selected-cube RotateTo/Open transition, four internal actions, panel activation, reverse Close/BackRotate, and return to idle. The main r6 matrix passes 93/93 assertions with zero console/page errors; canonical game-client state/capture lives under `output/web-game/cubz-exact-layout/`, with full-page closed/open captures under `output/playwright/r6-restoration-final/`.
- Agent World API v2 final QA is complete. Permanent `npm run qa:agent-world` builds an independent 28-entity AI Garden only through `window.__GRAPHYSX__` and passes 26/26 assertions: schema/capability discovery, atomic construction, tag/type/spatial queries, structured observations, deterministic pause/step, stable-id update, pulse and cross-entity look-at behaviors, rejected transaction and rejected full-world rollback, hierarchy removal/undo, and save/clear/load, with zero console/page errors. Required game-client capture/state is clean under `output/web-game/agent-world-studio/`; visually inspected API-world captures are under `output/playwright/agent-world-api-v2/`.
- Final current-source production build passes. The complete r7 regression matrix passes 101/101 with zero browser errors after synchronizing its stale census assertion to the XML-audited live total of 50 scenes (3 RESTORED / 34 PARTIAL / 13 PIPELINE / 0 MISSING). Next engine increments: asset/GLB registry entities, mountable Nature/2D instrument adapters, interaction/constraint commands, and optional network collaboration metadata while preserving the v2 agent contract.
- Integrated the active Atmel Slide1 assembly into the main UI as its own `BallZ Concepts — Active Atmel Slide 1` non-race visit, separate from both playable BallZ races and the distinct 2011 Level1 mesh. Main navigation, project-mode navigation, BallZ concept backlog, structured state, Ball/edges/bounds controls, and exact/inferred evidence cards are live. The Slide 1 census row moves PIPELINE → PARTIAL, yielding 3 RESTORED / 33 PARTIAL / 0 REGRESSED / 16 PIPELINE / 0 MISSING across the same 52 de-duplicated scenes.
- Visible build advanced to `revival-2026.07.17-r7`. Integrated Slide1 QA verifies the correct 566v/552t revision, both exact source transforms, mass 5000, chase offset, placeholder-ring disclosure, diagnostics and main-menu reachability; the first r7 matrix passes 101/101 assertions with zero browser errors. Required main-app game-client capture/state is under `output/web-game/r7-ballz-slide1-main/`. A following presentation pass removes non-authored low-resolution cube-map backgrounds from Slide1 and CubZ inspection spaces while retaining environment lighting.
- 2026-07-17 Suzanne 2 forensics found exactly two byte-identical `Suzanne2.ASCII` copies (SHA-256 `164693E2...12C69`) and two byte-identical `Suzanne2.xml` copies (`70140AB6...1B52`), but no Suzanne 2 screenshot; `Suzanne1.png` is not borrowed as evidence. The active March 2017 `GamePlayScreen` + `GraphysX_1::BuildASCIIScene` pair defines a 40×40 arena with 313 `#/Z/z` walls plus two `s` effect walls (315 collision cubes), 15 rings, three 3-body chains, three 3-body pistons including hidden triggers, four lap posts, four 64-particle emitters, two CubX anchors, player `[10,0.5,6]`, exact finish/halfway segments, and three XML objects. The active source actually advances after 2 pickups despite authoring 15 rings and has no lap victory target; both facts are preserved.
- Added deterministic `tools/convert-suzanne2-ascii.mjs`, generated `src/legacy/suzanne2-ascii-scene.json`, isolated `src/suzanne2-ascii-environment.ts`, `suzanne2-ascii-preview.html`, `scripts/qa-suzanne2-ascii.mjs`, and `SUZANNE2_RECOVERY.md`. The converter preserves the XML Airplane (29,298v/9,766t, seven material groups and six exact BMP maps), static mesh-loader bind pose of BonedGate (96v/188t plus documented 9×200-key animation), and the active SuperCage ZombieKiller player (29,024v/58,128t plus 0.225 FireArrow sphere). Host-global Voie Lactée/spline flyer/humans/cone/fire remain excluded from the isolated level. Full TypeScript passes; required game-client state reported `ready` with exact counts and no error file; permanent Playwright QA passes 25/25 with zero console/page errors and visually inspected overview/source-camera captures under `output/playwright/suzanne2-ascii/`.
- 2026-07-17 CubZ TVA forensics replaced the former animation-decoding unknown with deterministic evidence. `tools/convert-cubz-tva.mjs` decodes the exact MHI2 hierarchy, MANS ranges/timing, and counted MANI position/quaternion/scale arrays from `CubeRot.tva` (`8ADBA22A...AE7B4B`) and `CubeOpen.tva` (`CCB1ED3B...0CA27B`) into `src/legacy/cubz-tva-animations.json`; `tools/verify-cubz-tva.mjs` passes against both raw files.
- `CubeRot` contains one 0..220 master animation plus seven 28-frame selection ranges at 30 fps; only node 1 `GlobalCube` changes materially, with 211 optimized quaternion keys and exact keys at every range boundary. `CubeOpen` contains one 0..50 animation at 30 fps; only `Right08`, `Top08`, and `Left08` rotate, each with 51 exact quaternion keys, while their position/scale tracks remain constant. No TV3D-to-web handedness or engine interpolation is inferred.
- Six surviving CubZ sources collapse to two unique implementations: five byte-identical baseline `CubZ.cpp` copies and the enhanced `CubZ (2).cpp`. The C++/binary cross-check exposes a real archive defect: forward selection uses valid IDs 1..7, while `BackRotate()` requests `iSelectedCube+1` (2..8), shifting every reverse range and exceeding the decoded 0..7 table for cube 7. The 16 related `Media/CubXActor` TVAs all have distinct hashes and belong to a separate individual-actor revision; none is a target alias. Full evidence and integration boundaries are recorded in `CUBZ_TVA_AUDIT.md`.
- Added isolated `src/cubz-tva-animation.ts`, which samples the seven `CubeRot` selection ranges and the three animated `CubeOpen` Box08 panels at the authored 30 fps. Integer-frame samples preserve the decoded float32 quaternion exactly; fractional frames use deterministic normalized shortest-arc SLERP and disclose that interpolation as an inference. Source-order TV3D components remain unconverted until actor-to-browser handedness is visually established.
- Repaired reverse playback now traverses the same range selected for forward playback, while structured state still reports the archived `iSelectedCube+1` request and its invalid selection-7 ID 8. `cubz-tva-animation-preview.html` makes the range, direction, exact-key/SLERP status, and all three Box08 tracks inspectable without touching the shared application UI.
- Permanent `scripts/qa-cubz-tva-animation.mjs` passes 109 assertions with zero browser errors across exact endpoints, forward/reverse symmetry, complementary time samples, fractional SLERP normalization, archived-defect disclosure, all open-panel tracks, deterministic stepping, and invalid selection rejection. The required game client and three Playwright captures were visually inspected, `npx tsc --noEmit` and the full production build pass, and no shared prototype/race/content/CSS integration file was changed.
- 2026-07-17 Agent World composition/collaboration pass: added isolated `src/agent-world-prefabs.ts` with five discoverable multi-entity recipes—Luminous Tree (6), Signal Beacon (8), Portal Arch (7), Orbital Sculpture (12), and Habitat Pod (8). `spawnPrefab()` and the atomic `spawn-prefab` command expand these into ordinary stable-ID v2 entities with configurable transform, palette, and semantic tags.
- Added actor-aware `commit()` with intent, optional optimistic `expectedRevision`, atomic commands, serializable receipts, conflict events, and incremental `history()`. Stale observations are rejected without changing revision, entities, or accepted history. The Studio now exposes the prefab library, one-click Collaborative Prefab Plaza, actor-attributed events, collaboration history, and current code examples. TypeScript passes; targeted browser/build/regression QA remains for this pass.
- 2026-07-17 r7 reconciliation integrated three additional source-backed visits into the main application: the active Atmel Slide1 assembly, six-of-seven-object `MyWorld — Copy`, and the distinct Suzanne 2 40×40 authored environment. Each has first-class navigation, isolated controls/state, explicit source-versus-inference boundaries, and a solid inspection background that avoids magnifying unauthored low-resolution skyboxes.
- CubZ now consumes the decoded `CubeRot.tva` and `CubeOpen.tva` clip ranges and durations in the main state machine. Its exact eight hit-box centers and 58-unit boxes remain source-authored; selection 0 opens directly, selections 1–7 use their seven exact 28-frame ranges, the panel clip uses frames 0–50 at 30 fps, and reverse playback intentionally repairs the archive's broken `iSelectedCube+1` lookup by traversing the same selected range backward. Procedural geometry is still an explicitly labeled adapter pending original actor/button conversion and validated TV3D handedness.
- Corrected the distinct-scene census from 52 to 50 after byte evidence proved `SlideLong1.TVM` is the already-counted BallZ 2011 Level1 mesh and `pipe1.tvm` is a FlightX component rather than a separate scene. Current registry/census/UI totals agree at 3 RESTORED / 35 PARTIAL / 0 REGRESSED / 12 PIPELINE / 0 MISSING / 0 RESEARCH.
- Final production regression for this wave passes 129/129 assertions with zero console/page errors under `output/playwright/r7-restoration-final/`. It covers the complete 50-scene index; selector, Nature/Agent/Common/XML/Slide/Suzanne/CubZ isolation; exact CubZ open/close paths including selection 0; all three classic three-lap races; and Suzanne 1 completion. Visual inspection confirmed the closed and unfolded CubZ layouts in `12-cubz-exact-closed-layout.png` and `13-cubz-unfolded-panels.png`.
- 2026-07-17 CubX/CubZ UI revival milestone 1: `src/cubx-menu-animation.ts` now samples the converter-owned `src/legacy/cubx-anim.json` `open` clip across source t=0..100 (linear position/scale plus shortest-arc quaternion interpolation). The main CubZ lab maps its open phase directly to those samples and close to the same samples in reverse, replacing the invented spread-position/euler choreography while preserving the existing CubZ selection-range flow.
- The live adapter applies `Scene_Root` plus every available `Bo_te01..08` track to the procedural eight-cube rig. The generated data currently omits `Bo_te05`; cube 5 deliberately remains at bind pose and the structured/UI fidelity telemetry reports that boundary. Authentic CubX idle rotation is now 20°/s per axis and snap motion is 80°/s per axis. `npx tsc --noEmit` passes after this milestone.
- 2026-07-17 CubXActor lineage forensics decoded all 16 byte-distinct TVAs under `Media/CubXActor` into `src/legacy/cubx-actor-lineage.json`, including exact MHI2 hierarchy/matrices, MAMD geometry summaries, 30 fps MANS timing, and substantive MANI keys. `tools/verify-cubx-actor-lineage.mjs` passes all 16 raw hashes and structural/timing invariants. `CubXGet2..8` and `CubXOpen` each carry 2,264 vertices / 2,750 triangles; `CubXRot1..7` carry 2,148 / 2,688; `CubeOpensolo` carries 140 / 74.
- Companion click TVMs prove spatial order `Box05, Box04, Box03, Box02, Box08, Box06, Box07, Box01`, while the byte-duplicated host maps clicks 1..7 to `Get2..8` and click 8 to an uninitialized actor slot. The full open actor replaces `Box05` with `Cube/Right/Top/Left`; the homologous solo export calls its panel lineage `Box08/Right08/Top08/Left08`. The three panel geometry payloads are byte-identical and animation keys agree within float32 export noise, proving shared lineage but unstable `BoxNN` naming.
- `CUBX_ACTOR_LINEAGE_AUDIT.md` records why this newer family cannot defensibly fill CubZ's missing click-to-Box label map, plus exact host position `(-400,500,0)`, ignored/commented Size 4 scaling, StdMat constants, 0..100 terminal holds, the missing `Media/CubX` path, the seven-slot/eight-click bug, the `DisableCubXButtons` enable bug, and empty close/deselect states. No shared UI/race/content/CSS integration file was changed by the audit; a concurrent strict JSON tuple-cast error in `cubx-menu-animation.ts` was repaired with runtime key validation, and `npx tsc --noEmit` passes.
- 2026-07-17 particle/effect audit separated authored GraphysX features from adjacent tutorials/reference material. The resource folder contains 19 compiled TVP filenames / 17 unique hashes, plus 16 readable TVPJ documents with 29 exact emitter configurations and one attractor. `explosion1.TVP` aliases `explosion_01.TVP`; only that preset has located GraphysX execution paths. Three unique binaries (`explosion3`, `impact1`, `shot1`) remain opaque, while `expl1.TVPJ` references a missing `clumpy_blurry.dds`. `PARTICLE_EFFECT_ARCHIVE_AUDIT.md` records every readable capacity, generation interval, shape, direction/random/power, gravity, lifetime, texture, change/blend/depth flag, keyframe, programmatic ring/Suzanne/Human emitter, and active/disabled callsite classification.
- The DirectX `ParticleEng.cpp` and 2008 `Particle Engine Proj.pdf` are external tutorial/report evidence, not GraphysX worlds. The TV3D Snowfall/Smoke port is derived from that tutorial and its only construction/update block is commented out. `CLEffect`, `ParticleEffect`, the TVP library, ring bursts, ASCII smoke, Human trail and animated flame billboards are reusable features inside scenes rather than additional scene-census rows.
- Added isolated `src/particle-effect-environment.ts` and `particle-effect-preview.html` for the strongest missing source-backed preset: the exact three-emitter `Explosion1.tvp` configuration (10/64/8 = 82 particles), byte-identical `Glow2.dds`/`smokey.dds`, one-shot behavior, archived host position `(-5,0,0)`, seeded fixed-step runtime and explicit source-versus-adapter telemetry. The environment defaults to one-shot; the standalone preview labels its optional auto-replay. No shared `race-scene.ts`, `prototype-app.ts`, `archive-content.ts`, shared CSS or root tracker was edited.
- Permanent `scripts/qa-particle-effect.mjs` passes 14/14 assertions: exact inventory fields, 82 active particles at deterministic 700ms, repeatable restart positions, pause semantics, one-shot completion, keyboard replay/restart, successful asset loads and zero browser errors. Required game-client release capture/state is under `output/web-game/particle-effect-explosion1-release/`; `output/playwright/particle-effect-explosion1/explosion1-700ms.png` and the release screenshots were visually inspected. Naive Three.js multiply produced invalid black DDS quads, so the exact archived COLOR/ADDALPHA enums are retained while the disclosed runtime adapter uses alpha/additive mapping; the closed-source TV3D blend-state recipe and RNG/integrator remain unavailable.
- Superseded particle integration TODO: the generalized readable/opaque preset library is now mounted under Engine & FX with Restart/Pause/Auto replay and structured fidelity state. The synthetic fountains remain separately labeled diagnostics, and only presets with located callsites claim that evidence.
- 2026-07-17 ObjectLibrary catalog revival converts the proven 61-entry editor grid without reinterpreting it as a village. `tools/convert-object-library-catalog.mjs` preserves every serialized action/transform/physics field and recovers 5 procedural objects, 40 exact-basename text-X assets and 2 exact TVMs; 11 files, 2 absent-source duplicates and the binary `port_crateshed.X` remain explicit non-geometry records.
- Generated `src/legacy/object-library-catalog.json` contains 240 X source meshes / 28,051 vertices / 16,658 triangles / 265 material groups plus exact Prisme (148v/169t/1 group) and Airplane (5,367v/9,202t/8 groups). All 71 texture outputs are hash-locked; 21 TGA files are losslessly decoded to PNG. Eleven exact source textures with at least 5% pure-magenta pixels receive a recorded shader-side legacy color-key adapter while source/output image bytes stay unchanged.
- Added isolated `src/object-library-catalog-environment.ts`, `object-library-catalog-preview.html`, and `src/object-library-catalog-preview.ts`. The browser exposes family/status filters, exact selection metadata, deterministic orbit/zoom/time state, and inspection-only labels at missing/binary serialized locations. It adds no proxy geometry and explicitly identifies the source as a catalog grid rather than a composed world.
- `tools/verify-object-library-catalog.mjs` passes raw source/texture hashes, all 61 exact authored records, geometry/group/index invariants and the 47 recovered / 13 missing / 1 unsupported boundary. Permanent `scripts/qa-object-library-catalog.mjs` passes 25 assertions with zero browser errors and visually checked overview/port/binary/missing/nature captures under `output/playwright/object-library-catalog/`; the required web-game client also passed ready state and orbit with no error file. Isolated strict TypeScript passes. Full build currently reaches only unrelated concurrent CubX/race errors and reports no ObjectLibrary error.
- 2026-07-17 Agent World prefab/collaboration final QA is complete. `npm run qa:agent-world` passes 41/41 assertions with zero browser errors, including exact recipe counts, a five-prefab 43-entity actor commit, customized 51-entity composition, semantic tags, incremental history, stale-revision rejection, duplicate-commit rejection, and atomic prefab rollback. The Studio action and collaboration inspector captures are under `output/playwright/agent-world-api-v2/`.
- The required game client reports the v2 prefab/collaboration capabilities and a clean deterministic world state under `output/web-game/agent-world-prefab-collaboration/`. The current production build passes, and the complete r7 matrix passes 129/129 assertions with zero browser errors under `output/playwright/r7-restoration-final/`; its CubZ wait now covers the recovered snap-plus-open and close-plus-return animation chains instead of asserting midway through a valid clip.
- The collaboration surface is intentionally transport-neutral: actor identity, intent, optimistic revision guards, receipts, conflicts, and history are stable local/runtime primitives. Network presence, shared persistence/transport, richer constraints/interactions, and an asset/GLB registry remain the next engine increments without changing the current v2 contract.
- 2026-07-17 CubX/CubZ UI revival: `src/cubx-menu-animation.ts` samples converter-owned `src/legacy/cubx-anim.json` across source t=0..100 (linear position/scale, shortest-arc quaternion interpolation). The main CubZ open phase now applies `Scene_Root` plus all available `Bo_te01..08` tracks and close samples them backward, replacing the hand-authored spread/euler adapter. `Bo_te05` is absent from the generated file and deliberately remains at bind pose with an explicit fidelity disclosure. Idle/snap constants are 20°/s and 80°/s per axis; the global 10-second screensaver orbit remains single-owned.
- The CubZ BallZ section is now a persistent Fire / Classic / Revival selector. Fire uses the recovered BallFire mesh with Revival physics, Classic selects the recovered BallShell/BallCtrl presentation plus 2015 gravity/brake behavior, and Revival keeps the modern fluid-shell setup. `B` cycles all three and the selector is mirrored in structured state.
- Added the source red CubX sun as a raycastable 3D `FlightX` control plus an accessible mirrored action. It selects and launches the existing `flightx-pipe` race, waits for lazy assets when necessary, and returns through the recovered-world flow. `RECOVERED_WORLD_IDS` remains synchronized to the requested seven unchanged ids.
- 2026-07-17 CubXActor lineage inspection now includes generated exact render geometry in `src/legacy/cubx-actor-inspection-geometry.json`: all 16 actors reference eight deduplicated MAMD payloads, while `CubXMesh.tvm` and all eight `CubXBtnN.tvm` companions retain exact indexed arrays. The verifier now checks payload coverage/counts and all nine companion hashes in addition to the prior 16-asset lineage/timing/click invariants.
- Added isolated `src/cubx-actor-lineage-environment.ts` and `cubx-actor-lineage-preview.html`. Closed, Get2..8, Rot1..7, full Open and solo Open can be inspected with exact hierarchy/geometry and authored ranges at 30 fps; Get/Open terminal holds remain visible. Stored MANI keys are exact, between-key linear/shortest-arc interpolation is disclosed, and raw TV3D source order is deliberately not presented as a validated handedness conversion.
- The inspector is explicitly separate from CubZ. Exact click proxies retain spatial order `Box05, Box04, Box03, Box02, Box08, Box06, Box07, Box01`; click 8 is a selectable red defect record for uninitialized slot 7 outside the 0..6 render loop and never invents an actor. Rot and solo clips remain labeled host-unused because no literal host calls survive.
- Permanent `scripts/qa-cubx-actor-lineage.mjs` passes 88 assertions with zero browser console/page errors across every clip, counts/ranges/holds, deterministic 30 fps playback, all valid click flows, click 8, toggles and invalid inputs. Four Playwright views under `output/playwright/cubx-actor-lineage/` and the required client view under `output/web-game/cubx-actor-lineage/` were visually inspected; geometry, UI, terminal-hold telemetry and clean click-proxy toggle are coherent. `CUBX_ACTOR_LINEAGE_INSPECTION.md` records the minimal mount/playback API and the no-CubZ-binding boundary.
- 2026-07-17 remaining BallZ slide/track gallery conversion now exports exact indexed geometry for Slide1A, Level.Slides, Level.Steps, SlideBump, SlideBumpGridTex and BallZTrack1. MGRP-416/MGR4-172 records retain raw hashes and color slots; Level.Steps proves group bindings to grass/concrete/wood and GridTex proves its EarthGri binding. Four browser texture copies are byte-hash locked. SlideLong1 and pipe1 remain excluded aliases, and the distinct compact StockRoom Slide1 revision remains audited outside this requested gallery.
- Added isolated `src/ballz-track-gallery-environment.ts` and `ballz-track-gallery-preview.html` with six selectors, source/diagnostic materials, overview/top/orbit camera state, reversible normalization, edge/bounds diagnostics, structured evidence, and no gameplay. Visual QA caught that the four StockRoom/Blender TVMs carry all-zero source normals: exact arrays remain preserved, while the display computes normals under an explicit adapter flag. Slide1A and BallZTrack1 retain their valid source normals unchanged.
- Permanent `scripts/qa-ballz-track-gallery.mjs` passes 88 assertions with zero browser errors across all six meshes, exact counts/topology/materials, all host boundaries, texture hashes/load state, Bump revision distinction, reversible display conversion, normal adapters, diagnostic toggles, camera profiles, deterministic orbit and alias rejection. Seven full-page screenshots under `output/playwright/ballz-track-gallery/` plus the required client capture/state under `output/web-game/ballz-track-gallery-final/` were visually inspected. `BALLZ_TRACK_GALLERY_RECOVERY.md` records the mount/selection API and recommends one six-selector BallZ Concepts gallery, not six playable races.
- Superseded gallery integration TODO: `BallzTrackGalleryEnvironment` is mounted as one BallZ Concepts/Archive Gallery destination. Every internal entry remains `PARTIAL`; SlideLong1 and pipe1 remain excluded aliases, the compact StockRoom Slide1 revision stays separate, and no active-Slide1 gameplay was borrowed for Slide1A.
- 2026-07-17 CubX/CubZ UI revival final QA recovered the exact source camera start `(400,250,-2000)` from `AtmelCubx/TV3DMoteur.cpp` and applies the authored per-frame `(-10,+5,+10)` fly-in with a clamped `(-75,725,-350)` destination. The runtime converts that source-relative position at the existing presentation boundary, then hands control back to the normal visit camera; it does not duplicate the global 10-second screensaver orbit.
- Permanent `npm run qa:cubx-ui` passes 28/28 browser assertions with zero console/page errors. It verifies live `cubx-anim.json` t=0..100 forward/reverse sampling, the missing `Bo_te05` disclosure, 20°/s idle and 80°/s snap constants, camera arrival, all three persistent BallZ presets with recovered mesh readiness, and a real WebGL pointer raycast on the red SUN cube that launches `flightx-pipe`. The required game client state/capture and visually inspected Playwright frames live under `output/web-game/cubx-ui-revival/` and `output/playwright/cubx-ui-revival/`.
- 2026-07-17 Dominus source-asset recovery now has a deterministic 65-record gallery, grouped only by the seven proven filename families: bush 4, camp 4, character 8, grass 5, port 28, weapon 6, and tree 10. Sixty-three text DirectX X files recover into 283 source mesh parts / 40,836 vertices / 26,317 triangles / 308 material groups; `port_crateshed.X` and `renzokscale.X` remain honest binary-X inspection records with no proxy geometry.
- `tools/convert-dominus-asset-gallery.mjs` generates the small manifest plus one lazy payload per recoverable source and hash-locks all 83 referenced textures. Thirty-one TGA images are losslessly decoded to browser PNG; the other 52 JPG/PNG files are byte-identical copies. Twenty-one exact source textures receive a disclosed shader-side magenta color-key adapter while their source/output image bytes remain unchanged.
- Added isolated `DominusAssetGalleryEnvironment`, standalone preview, structured family/status filters, source metadata/texture evidence, deterministic orbit/zoom/reset, and explicit `SOURCE ASSETS ONLY / NO SURVIVING COMPOSITION` framing. It is not a village/world reconstruction and supplies no invented terrain, water, camera, layout, navigation, interactions, or BallZ placement.
- `tools/verify-dominus-asset-gallery.mjs` passes every raw source/payload/texture hash and geometry/material invariant. Permanent 65-asset browser QA passes 298 assertions with zero console/page errors; the required game client proves held-key deterministic orbit changes with `ready=true` and no error file. Keep Dominus Village/Port at PIPELINE until authored composition evidence appears; integrate this only as an archive/source-asset gallery through the documented minimal API.
- 2026-07-17 main-app reconciliation supersedes the earlier CubX/CubZ animation conflation: `cubx-anim.json` / the older CubXActor `CubXOpen.tva` is no longer imported or sampled by the CubZ menu. Visible rotation now applies the exact selected `CubeRot.tva` `GlobalCube` range for 0.933 seconds; visible opening applies `CubeOpen.tva` frames 0..50 at 30 fps to the procedural Right08/Top08/Left08 panel adapters, and both paths traverse the same exact range in reverse. The old CubXActor family remains available only through its separate evidence inspector.
- Integrated the exact CubXActor lineage inspector into the main Archived Worlds menu. It renders exact Closed/Get2..8/Rot1..7/full Open/solo Open geometry, all eight exact click meshes, 30 fps ranges and terminal holds, and preserves click 8 as an uninitialized slot outside the host render loop. Main text/debug state and controls expose clip family, pair, play/reverse/frame stepping, click evidence, proxy visibility and diagnostic colors without changing the 50-scene census or claiming a CubZ mapping.
- Integrated the ObjectLibrary 61-record catalog and exact-config `Explosion1` particle preset into the main app, superseding the earlier isolated-only TODO. ObjectLibrary remains a filtered catalog—not a village/world—and Engine & FX keeps the three modern diagnostic fountains labeled separately from the archived 10/64/8 one-shot preset.
- 2026-07-17 full Archived Particle Preset Library pass added deterministic `tools/convert-particle-preset-library.mjs`, `src/legacy/particle-preset-library.json`, isolated `ParticlePresetLibraryEnvironment`, and the standalone preset browser/player. All 16 readable TVPJ presets / 29 emitters / one attractor preserve exact serialized configuration and surviving DDS bindings; 19 compiled filenames deduplicate to 17 hashes with both exact alias pairs retained as evidence.
- The selector keeps `expl1` and `fire1` readable-source-only, gives missing `clumpy_blurry.dds` a visible magenta-X diagnostic rather than an invented texture, and keeps `explosion3`, `impact1`, and `shot1` as selectable opaque evidence with zero guessed emitters. The library is explicitly an Engine/FX feature, not a World/BallZ scene or census row.
- Permanent `scripts/qa-particle-preset-library.mjs` passes 97/97 assertions with zero failed responses and zero console/page errors across every readable and opaque record, aliases, counts, deterministic restart, pause/replay/restart/selection controls, and missing-texture behavior. Every readable preset plus representative opaque/source-only views was visually inspected under `output/playwright/particle-preset-library/`; the required web-game client produced clean structured state/screenshots under `output/web-game/particle-preset-library-final/`.
- 2026-07-17 main-app content reconciliation integrates the six-source BallZ slide/track gallery, the 65-record Dominus source-asset gallery and the complete archived particle preset library. The six BallZ entries are evidence-bounded non-gameplay visits, and Dominus is explicitly source assets only; neither gains invented race/world composition. The scene census remains 50 distinct records and now reports 3 `RESTORED`, 42 `PARTIAL`, 5 `PIPELINE`, 0 `MISSING`.
- The BallZ gallery main-app lighting boundary was corrected after required-client inspection exposed black geometry under the archived-room light override. The final capture shows the exact geometry and bound materials clearly; the Dominus and particle-library main captures also report ready state without error files.
- Final integrated r7 regression passes 218/218 assertions with zero console/page errors under `output/playwright/r7-restoration-final/`. It covers the 50-record census, grouped navigation, selectors, standalone environments, ObjectLibrary, isolated CubXActor and CubZ flows, six-source BallZ gallery, 65-record Dominus gallery, all archived particle-library classifications and controls, classic races, and Suzanne completion. A first run caught a transient preview-server reset while fetching the present `Alien02_diff.bmp`; the unchanged full rerun served and decoded it cleanly.
- 2026-07-17 r8 camera-collision increment: the active BallZ/vehicle chase camera now raycasts from outside the player body to its desired position through the live Cannon world. A blocking body shortens the camera with a 0.35-unit surface margin; unblocked views retain the requested distance. `render_game_to_text` and the debug snapshot expose active/hit-body/desired/resolved telemetry, and leaving gameplay clears stale collision state.
- Permanent `npm run qa:camera-collision` passes 9/9 assertions with zero browser errors. It uses the tall authored ZombieKiller south wall to prove collision shortening, then the same yaw at a high pitch to prove the unblocked distance is unchanged. Both full-page captures under `output/playwright/r8-camera-collision/` were visually inspected. Standalone visit/orbit cameras remain the next collision scope; the extreme low-angle blocked view is intentionally close and should receive presentation tuning if it becomes a common player gesture.
- 2026-07-17 Math Game scope correction: a fresh audit of `Scene3D/MathGameScreen.cpp` and ArduinoGUI `MathScreen.cs` confirms that neither surviving branch implemented goals, scoring, success/failure, or progression. The live screen now says so instead of treating nonexistent gameplay as forgotten recovery work; its presets are labeled modern inspection aids. The 10,000 instances, 100 z-lanes, parabola/slope formulas and lane color gradient are marked source-ported with only browser world scale disclosed as adapted.
- 2026-07-17 Agent World usability pass adds three one-call starter recipes without changing the v2 schema: Prefab Plaza (43 entities), Glow Garden (45), and Signal Outpost (41). `starters()` discovers them and `loadStarter(id, { id?, label? })` creates a fully lit world whose ordinary prefab/entity parts remain editable through the existing API. The Studio exposes the same three choices as compact one-click cards; no network, asset-loader, or new scene DSL was introduced.
- Starter-world final QA is complete: `npm run qa:agent-world` passes 49/49 assertions with zero browser errors across discovery, custom identity, exact expansion counts, semantic roots/lights, invalid-id safety, both visible starter cards, and all earlier v2 collaboration/rollback cases. Glow Garden and Signal Outpost captures were visually inspected under `output/playwright/agent-world-api-v2/`; the required game client is clean under `output/web-game/agent-world-starters/`; production build passes; and the full integrated r7 matrix remains 218/218 with zero errors.
- Keep the next Agent World increment similarly narrow. A small interaction vocabulary (for example click/toggle and proximity-triggered visibility) would add more user value than networking or a general asset pipeline right now; GLB registration should wait until an actual project asset requires it.
- 2026-07-17 Agent World interaction pass implements the narrow first half of that recommendation: one serializable `toggle-visibility` interaction, `interact(id, interactionId?)`, and an atomic `interact` command. Receipts report source/action/label plus resulting target visibility; entity state/export/save/load preserve interaction metadata; invalid or compound commands use the existing rollback path. The same runtime method now backs agent calls, accessible Studio buttons, and direct WebGL raycast clicks.
- Prefab Plaza, Glow Garden, Signal Outpost, and the default constellation each expose one obvious interactive centerpiece while keeping their entity counts unchanged. Conditions, callbacks, arbitrary scripting, proximity triggers, networking, and asset loading remain deliberately outside this increment.
- Interaction final QA is complete: `npm run qa:agent-world` passes 58/58 assertions with zero errors across discovery, receipts, default-action convenience, accessible controls, real WebGL raycast picking, invalid-action safety, and transaction rollback while retaining every earlier v2 test. The hidden-axis direct-click capture under `output/playwright/agent-world-api-v2/00-direct-3d-interaction.png` was visually inspected; the required game client is clean under `output/web-game/agent-world-interactions/`; production build passes; and the full integrated matrix remains 218/218 with zero browser errors.
- Next Agent World priority: pause and use the interaction in an actual small experience before adding another vocabulary type. If a second primitive proves necessary, a distance/proximity trigger can reuse the same target/action receipt model; avoid arbitrary scripts or networking until a concrete world needs them.
- 2026-07-17 StockRoom XML artifact audit resolves `BaseScene.xml` and `test1.xml` as serializer/schema fixtures, not distinct assembled scenes. `BaseScene.xml` is 18 byte-identical `PHYSICCUBE` records at one exact overlapping transform; `test1.xml` is one origin unit cube, with its two known copies differing only by newline. No authored camera, lighting, world layout, interaction, or gameplay survives in either file, so the scene census recommendation is an independent delta of -2 `PIPELINE` / -2 total (current 3/42/5/50 baseline becomes 3/42/3/48), while both remain preserved under an XML Serializer Artifacts inventory.
- Added deterministic `tools/convert-stockroom-xml-artifacts.mjs`, exact generated data, isolated `StockroomXmlArtifactEnvironment`, standalone preview, verifier, permanent browser QA, and `STOCKROOM_XML_ARTIFACT_RECOVERY.md`. The inspector preserves the 18 exact overlaps instead of spreading them, makes the same-folder `twoway.jpg` candidate opt-in because the token-to-file binding is unproven, and discloses all grid/light/camera/neutral-material adapters. Verification passes 72 source/hash/schema assertions; browser QA passes 34 assertions with zero errors; the required game client is clean; all five neutral/candidate/test views were visually inspected. No main integration or census/tracker file was edited in this isolated pass.
- 2026-07-17 remaining BallZ2015 XML audit separates `MyWorld.xml` and `TestWorld.xml` from both `MyWorld - Copie.xml` and the StockRoom serializer artifacts. They are two distinct serialized test compositions, not finished discoverable worlds: MyWorld is a broken editor save with 2/4 exact renderable records (10,261v/11,168t high-detail airplane plus the tiny source-dimension physics cylinder), while TestWorld is a loader coverage fixture with 5/8 exact records (four primitives plus the 5,367v/9,202t AirplaneLP). MyWorld's two Suzanne X paths are malformed `DUPLICATE` targets under the document's own embedded action table and are deliberately not loaded; TestWorld's bush source is absent and its two duplicate targets are empty.
- Added isolated deterministic `tools/convert-ballz-xml-worlds.mjs`, `src/legacy/ballz-xml-worlds.json`, `BallzXmlWorldsEnvironment`, standalone preview, source verifier, permanent browser QA, and `BALLZ_XML_WORLDS_RECOVERY.md`. The audit proves the later C++ enum drift, generic MyWorld editor buttons, absent TestWorld literal callsite, exact primitive factory dimensions/orientation, 15,628 decoded TVM vertices / 20,370 triangles, eight byte-identical Archive/StockRoom asset-copy pairs, and seven byte-identical browser texture outputs. Required client captures report MyWorld 2/4 and TestWorld 5/8 ready with zero errors; browser QA passes 28 assertions with zero errors, and overview/exact-object focus captures were visually inspected. Main UI/race/content/census files were not edited.
- Census recommendation for integration: retain both as distinct records and promote each `PIPELINE -> PARTIAL` only after mounting this exact evidence visit. Their serialized spatial compositions are materially richer than BaseScene/test1 schema fixtures, but UI must label MyWorld as a broken editor composition and TestWorld as loader coverage—not as playable races or finished exploration worlds. Neither merits `RESTORED` while 2/4 and 3/8 records remain unresolved and no authored camera/light/physics-world/interaction configuration survives.
- 2026-07-17 CubX source-constellation milestone adds isolated `src/cubx-satellite-shell.ts` with the recovered System/Tools/Earth/EarthGrid/Fleche/Sun assets, source transforms, MenuManager limits (48 buttons/16 levels), wildcard tags 666/777, and exact visibility semantics. One disclosed 0.005 browser presentation scale preserves the constellation while fitting the lab camera; source coordinates remain untouched in structured state.
- The live CubZ lab now renders and raycasts that shell. Decoded `fleche` geometry supplies the Arrow; Box/Earth/Sphere and unavailable source icon textures remain explicit procedural adapters. System is inspect-only because its recovered explosion destination is unavailable, Earth is inspect-only because its handler is empty, Tools opens the recovered one-car Impreza Car Scene, Arrow restores level 0/closes an open CubZ menu, and Sun still launches unchanged `flightx-pipe`. The workbench mirrors all four satellite actions and documents their source coordinates/tags.
- `RECOVERED_WORLD_IDS` remains exactly synchronized to the seven requested unchanged ids: `world1-recovered`, `map1-2011`, `piste-ovale`, `piste-ovale-cobra`, `slide-2008`, `flightx-pipe`, `dominus-port`. Ledger/forgotten/gap docs now describe the completed ball selector, sun launch, separated CubXActor/CubZ animation families, and the satellite-shell adapter boundary without claiming the older 80°/s CubXActor snap applies to CubZ's decoded reverse range.
- Permanent `npm run qa:cubx-ui` was reconciled to the decoded CubZ TVA family and now passes 50/50 assertions with zero browser/page errors against the successful static build. It verifies exact satellite coordinates/tags, Fleche readiness, CubZ open/close ranges, all three Ball presets, System/Earth evidence boundaries, Tools→fully loaded Car Scene and archived Impreza gravity drop, Arrow→level 0, and a real WebGL Sun raycast→FlightX. Screenshots under `output/playwright/cubx-ui-revival/` were visually inspected. The required game client also captured/inspected the final 0.005-scale constellation with clean structured state.
- Final whole-app build/typecheck must be rerun after the parallel Agent World interaction edit settles: the latest attempt was blocked only by its mid-write missing `resolveInteractions`/`validateInteraction`/`interact` members and required interaction fields. Do not repair that other lane from this milestone. C: also reached zero during concurrent artifact generation; disposable CubX QA folders were recoverably moved to `E:/GraphysX-codex-temp/cubx-qa-20260717`, while the permanent Playwright report was regenerated in-repo.
- 2026-07-17 GT4 / Low Cobra vehicle-pack audit proves both model packs are readable while preserving their `PIPELINE` boundary. GT4 contains 14 3DS objects / 10,740 point records / 8,345 faces / 2 materials and its TVM contains 12,916 vertices / 8,345 triangles. Low Cobra contains 10 objects / 6,961 point records / 3,266 faces / 7 materials / seven exact texture references and its TVM contains 7,298 vertices / 3,266 triangles; the two extracted Low_Cobra.3DS paths are byte-identical.
- The directly relevant host evidence is narrower than the existing web experiment suggests: `QTY_CARS` is 1, its name list contains only Impreza, the selector click callback is empty, and `CLVehicule` hard-codes Impreza chassis/wheel/texture paths. Its RWD/front-steer/suspension constants are not source bindings for GT4 or Cobra. The hidden `piste-ovale-cobra` race therefore needs relabeling as a modern speculative showcase: it currently cites authentic `Vehicule.cpp` handling, substitutes unreferenced `Cobra_Blue.tga` for the embedded `COBTEX.TGA` reference, and applies newly authored Cannon/Impreza rig values.
- Added isolated `vehicle-pack-inspector.html`, reusable `loadArchivedVehicle3ds(spec)` API, deterministic non-writing source verifier and `VEHICLE_PACK_ARCHIVE_AUDIT.md`. The runtime loader preserves exact objects/faces/UVs/per-face materials/textures with disclosed Z-up→Y-up conversion, no size normalization, and a single disclosed GT4 black-diffuse visibility override. Verifier passes 11/11, standalone browser QA passes 31/31 with zero errors, required client state/capture is clean, isolated build passes, and whole-project strict TypeScript now passes.
- 2026-07-17 r8 main integration mounts the remaining BallZ XML evidence visit, StockRoom serializer-artifact inspector, and exact GT4/Low Cobra source gallery as three deliberately separate destinations. MyWorld and TestWorld advance `PIPELINE -> PARTIAL` only because their exact 2/4 and 5/8 records are now reachable; malformed duplicate targets and missing bush remain explicit. BaseScene/test1 are removed from the scene census and remain visible under Recovery Status with `distinctAssembledScene=false`. GT4/Cobra remain `PIPELINE` as vehicles with selector/physics/playable all false.
- The scene census is now 48 distinct records: 3 RESTORED / 44 PARTIAL / 1 PIPELINE / 0 MISSING. The only remaining scene-level PIPELINE record is Dominus Village/Port, because its 65-source asset gallery is not an authored world composition. The running Home/index copy, `archive-content.ts`, `ARCHIVE_SCENE_CENSUS.md`, and `ARCHIVE_REVIVAL_TRACKER.md` use the same totals.
- The dormant `piste-ovale-cobra` adapter is removed from the recoverable/player roster and relabeled `Speculative Adapter`; it no longer cites Cobra as archive-authored vehicle physics. The exact source gallery keeps detached 3DS object pivots/components visible instead of welding or rearranging them, so apparent floating GT4/Cobra parts are source evidence rather than a new assembled-driving claim.
- Final r8 verification is clean: BallZ XML verifier 36 invariant groups; StockRoom artifact verifier 72 assertions; vehicle source verifier 11 assertions; chase-camera QA 9/9; strict TypeScript and production build pass. The integrated `npm run qa:r8` matrix passes 247/247 assertions with zero console/page errors and visually inspected Home, GT4, Cobra, both XML documents, both serializer artifacts, Math Game controls, existing archive galleries, CubZ/CubX, and race captures under `output/playwright/r8-restoration-final/`.
- Required `develop-web-game` client run is clean under `output/web-game/r8-final-integrated/`: both iterations report MyWorld ready at 2/4 exact records, no error file exists, and the latest canvas capture was visually inspected. `render_game_to_text()` now reports the three new standalone states and their local camera/control/evidence boundaries.
- Math Game now has an always-visible Reset 3D View control beside its live molecule chip, plus drag-to-orbit / wheel-to-zoom help. Its structured state exposes the local camera and correctly omits hidden BallZ race/player data from every archive lab. Full QA verifies real pointer-camera movement and deterministic reset; the fresh required-client run under `output/web-game/r8-final-math-controls-clean/` is visually inspected and contains no error file. A prior capture on the old dev server recorded only a transient Vite HMR WebSocket timeout and was superseded by this clean-server rerun.
- Next restoration priorities: (1) tune the extreme close-up presentation of chase-camera collision without weakening wall avoidance; (2) finish camera/light/material/handling fidelity for the three classic ASCII races and Suzanne 1; (3) recover authored Dominus composition evidence before upgrading its only remaining PIPELINE scene; (4) bind additional vehicles only if a source-backed selector/physics path is found, otherwise keep them exact asset-inspection entries; (5) recover exact Math/Editor GUI layouts only from surviving evidence rather than inventing absent game loops.
- 2026-07-17 final Dominus evidence pass exhausts the sole scene-level PIPELINE item without inventing its missing village. The only surviving multi-asset placement is the 28-port-row subset of the mixed 61-record `ObjectLibrary.xml`: all rows are enabled PHYSICXMESH thumbnails at Y=0, zero rotation, uniform 0.01 scale, and regular five-unit X/Z grid positions. Three known physical XML copies are byte-identical and therefore one logical document; no source loader callsite, authored scene document, camera, spawn, terrain/water, navigation, lighting, collision, or gameplay binding survives.
- Added isolated `DominusPortEvidenceEnvironment` and standalone preview. It renders all 27 decoded port text-X meshes at their exact ObjectLibrary transforms with exact material groups/textures, preserves `port_crateshed.X` as the sole unsupported binary-X transform plus an explicit amber diagnostic marker, totals 23,594 vertices / 13,860 triangles / 126 material groups, and exposes source order/transforms/hash/evidence boundary through deterministic state. Focus mode isolates the selected thumbnail because the source-local bounds overlap at catalog spacing; that isolation, the grid/lights/camera, Z-handedness display conversion, selection outline, and marker are all disclosed adapters.
- Dominus verification is clean: source verifier 355 assertions; browser QA 110 assertions with zero console/page errors; strict TypeScript passes. Exact-grid, main-docks, lighthouse, binary-boundary, and required-client screenshots were visually inspected. The required web-game client ran two clean ready-state iterations under `output/web-game/dominus-port-evidence-final/` with no error file.
- Integration recommendation: mount the new evidence environment as its own archive visit, relabel the existing race-scene port/ring layout as a modern curated visit, and advance Dominus `PIPELINE -> PARTIAL`—never `RESTORED`. This yields zero scene-level PIPELINE records while truthfully retaining the absent authored-composition boundary. Exact hooks and evidence are documented in `DOMINUS_PORT_EVIDENCE_RECOVERY.md`; shared app/census/tracker files were deliberately left untouched for conflict-free integration.
- 2026-07-17 Agent World Signal Trail builds the first tiny shared human/agent experience from the existing v2 vocabulary only. One click loads a 45-entity night world with three semantic `signal-node` beacons; either the accessible Studio actions, direct 3D picking, or `gx.interact(...)` reveals their ordinary `signal-energy` target entities and advances the derived 0/3 objective toward a stabilized portal. Restart is simply a fresh starter load. There is no hidden quest state, special script runner, networking layer, or new interaction type.
- Signal Trail final QA is clean: focused Agent World browser coverage passes 68/68 assertions with zero errors across agent/UI mixed play, exact target visibility, completion reversal, and restart; strict TypeScript plus production build pass; the full integrated r8 matrix remains 247/247 with zero console/page errors. The completed trail capture under `output/playwright/agent-world-api-v2/07-signal-trail-complete.png` and required-client Agent World capture/state under `output/web-game/agent-world-signal-trail-clean/` were visually inspected; the latter has no error file.
- 2026-07-17 Agent Level API foundation makes the BallZ ASCII editor a view over a canonical named-level library instead of a separate 11×11 UI island. `graphysx.agent-level/v1` documents carry id/label, revision, dynamic width/height, cell size, and semantic tiles; `gx.levels` now lists/creates/removes/opens levels, queries small regions, applies patch/fill/resize or atomic transactions with optional revision guards, undoes edits, imports/exports compact ASCII rows, and plays any named level through the existing race compiler. Persistence keeps up to 64 named levels and current dimensions up to 64×64 / 4,096 cells; ASCII is an adapter, not the canonical size or API model.
- Human and agent editing now share the same state: the Map Editor exposes the named library, creates blank 16×16 drafts, switches levels, reflects API edits/revisions, and writes clicks back through the level transaction path. `render_game_to_text()` reports the active level id/revision/dimensions/counts/rows, non-square race bounds are correct, `cellSize` reaches gameplay, and the 3D editor preview batches floor/wall tiles with instancing so larger grids do not create one floor mesh per cell. Strict TypeScript passes after this integration; browser and regression QA remain pending.
- Agent Level focused browser QA passes 29/29 with zero errors across a 24×18 programmatic Moon Base, atomic composition, region observation, ASCII export/import, rollback, revision conflict, three-level library, agent open, human click edit, cross-source undo, 30×20 resize, playable compilation, and reload persistence. Editor/library/grid and active gameplay captures were visually inspected. The required development client initially recorded aborted texture requests because an inherited 220-frame action burst closed during loading; a short-idle/real-pause rerun under `output/web-game/agent-level-api-clean-final/` is fully textured, has exact structured level state, and contains no error file.
- Full r9 regression twice exposed an unrelated nondeterministic CubXActor timing assertion: the source clip still advances at 30 fps, but the test counted real RAF frames occurring between separate Playwright `play` and `advanceTime` calls (isolated measurement: 2.022 frames before the intended 33-frame virtual step, 36.66 total). The assertion now measures the 1.1-second delta inside one browser task, preserving the exact 30-fps requirement without wall-clock contamination; a fresh whole-app rerun is pending.
- Agent Level release verification is complete: production build/strict TypeScript pass; Agent World remains 68/68 with zero errors; Agent Level remains 29/29 with zero errors; and the complete integrated r9 matrix passes 263/263 assertions with zero console/page errors after the deterministic CubXActor timing correction. The API stays intentionally bounded: semantic grid levels and their playable compiler are live now, while arbitrary free-positioned per-level 3D objects, chunk streaming beyond 4,096 cells, and remote multi-user storage remain future concrete increments rather than hidden framework work.
- 2026-07-17 The Funny Zigzagger is now the first complete Agent Level showcase. It is a normal persisted `graphysx.agent-level/v1` document (`funny-zigzagger`, 23×19, cell size 2.2) with alternating corridor walls, 17 rings, nine fire tiles, and eight ice tiles. It appears in the same named library as human/agent drafts, opens in the same editor, accepts the same region/patch/fill/transaction/undo operations, exports fire as `^` and ice as `~`, and plays through the ordinary level compiler.
- Fire and ice are first-class discoverable tiles across `MapEditorTile`, `gx.levels.tiles`, `gx.levels.tileSemantics`, summary counts, palette buttons, CSS, editor preview, ASCII import/export, and runtime compilation. Fire pads apply an outward/upward force; ice pads attract toward their center while bypassing ordinary ground grip so momentum remains slippery. Runtime pads use animated fire halos/flames and ice crystals, the gameplay HUD reports `FIRE — REPEL` / `ICE — ATTRACT`, and `render_game_to_text()` exposes `forceZone` plus `forceZonesTotal` for an observing agent.
- Funny Zigzagger verification is clean: production TypeScript/Vite build passes; focused Agent Level QA now passes 38/38 with zero errors, including API vocabulary discovery, showcase counts/ASCII, real fire launch and ice pull, telemetry, shared editor collaboration, resize/play/persistence; Agent World remains 68/68; and the clean full r9 matrix passes 263/263 with zero browser errors. Two earlier full-matrix attempts were invalidated by preview-network churn (`dist` changed under an older 4176 page, then an inherited 4177 server reset one asset); restarting preview after the final build produced the clean result.
- Required `develop-web-game` client completed two clean Map Editor iterations under `output/web-game/funny-zigzagger-final/`; both states report the two-level library and nine-tile semantic count structure, both captures are fully textured, and no error file was produced. The focused live-force capture is `output/playwright/agent-level-api-v1/00-funny-zigzagger-forces.png` and was visually inspected after adding the force ornaments.
- Keep the next increment concrete and small: use the current fire/ice vocabulary in another user-authored level before adding new force types. Directional vectors, per-zone strength, or larger/chunked worlds should only be added when an actual level needs them; arbitrary scripts remain deliberately outside this friendly level API.
- 2026-07-17 r9 restoration closeout reconciles the complete de-duplicated scene ledger at 48 reachable records: 3 `RESTORED`, 45 `PARTIAL`, 0 `REGRESSED`, 0 scene-level `PIPELINE`, and 0 `MISSING`. Dominus is no longer a hidden pipeline promise: its separate evidence visit renders the only surviving 28-row port placement exactly (27 decoded text-X meshes plus the explicit binary-X `port_crateshed` boundary), while the playable port tour is labeled modern curated work instead of the lost authored village.
- Main navigation is now family-first and source-separated. Home exposes BallZ races, non-race BallZ concepts/slides, FlightX, vehicle experiments, standalone 3D environments, CubZ, Math, editors, Engine & FX, recovery status, and Dominus evidence without forcing the old 28-mode wall open. Direct cards refresh ready state, the particle CTA opens the real archived-preset lab, and the build badge remains visible on desktop and mobile as `revival-2026.07.17-r9`.
- Gameplay/input closeout restores one shared keyboard/touch/gamepad path and deterministic visible pause. The mobile dock exposes movement, jump/action, pause and camera reset; pause releases held input and freezes simulation. Input/accessibility QA passes 15/15 with zero errors and visually inspected active/paused mobile captures under `output/playwright/r9-input-accessibility/`.
- Piste Ovale now uses the archived Impreza's authored +Z nose, three chassis material slots, grey mechanical undercarriage texture, source `EngineCtrl.Power = 3000`, and a disclosed 3.5% chassis-traction adapter for Cannon's spherical-wheel contact limitation. The banked road is excluded from wall-camera collision, its exact archived concrete stays readable under neutral revival lighting, and the default vehicle chase view is closer/lower. Deterministic QA moves the car more than one source unit in two virtual seconds and the camera suite passes 14/14 with zero errors under `output/playwright/r9-camera-collision/`.
- The final presentation pass keeps BallZ's internal directional arrow visible, widens the five low-resolution archive panoramas to 85–90° instead of magnifying them, and gives CubZ a readable inspection distance. CubZ still executes the decoded selection/open clips at 30 fps; only after the source clip finishes does a disclosed stable 2×2 panel adapter present New Note, Browse Notes, Pin Board and Archive. The closed constellation and open menu captures were visually inspected.
- Classic/Suzanne fidelity is locked by 74/74 source assertions plus a final combined 22/22 browser pass with zero errors (and 25/25 across the isolated case runs). Levels 1–3 retain 20 checkpoints, three laps and source radius 0.3; Suzanne retains 208 walls, 45 chains and 15 rings. Dominus source verification passes 355/355 and its isolated browser surface passes 110/110 with zero errors.
- Final integrated verification against the current r9 source passes 263/263 assertions with zero console/page errors under `output/playwright/r9-restoration-final/`; strict TypeScript and the 96-module production build pass. The required develop-web-game client made two clean ready-state iterations against the integrated Dominus evidence route under `output/web-game/r9-final-current/`, with no error file, and the exact-grid screenshot/state were visually inspected.
- The remaining work is now feature-level rather than forgotten scene-level work: original Math/NoteMgr CEGUI layouts are absent; Room 2's full HLSL behavior, standalone camera collision, generalized TVA/TVM/TVMO decoding, exact TV3D particle RNG/blending, true water reflection/refraction, broader editor gizmos/material/trigger tools, unbound vehicles/characters, performance budgets, audio/settings/remapping, save migration, deployment and an intentional Git baseline remain explicit in `ARCHIVE_REVIVAL_TRACKER.md`. None is silently promoted to restored without surviving evidence and a verified running implementation.
- 2026-07-17 Agent Entity bridge revives the original XML-era scene-builder meaning in the existing `graphysx.agent-world/v2` contract. Agents can now create visible `spline` and asynchronously loaded `model` entities, discover five converted archive models through `gx.assets()`, attach `follow-spline` by stable ID, and opt any supported primitive/model into real Cannon rigid-body simulation with `static`, `dynamic`, or mesh-controlled `kinematic` mode. Physics state reports mass/material/current velocities/sleeping; gravity is world-configurable; model IDs/URLs, paths, and physics intent survive JSON export/save/load. Root-only physics and kinematic-only transform behaviors keep the API friendly and deterministic.
- Focused `npm run qa:agent-entities` passes 26/26 assertions with zero browser errors across model discovery/loading, static/dynamic/kinematic complex models, six-point closed spline motion, dynamic gravity/collision/bounce, live velocity edits, invalid-combination rejection, export/save/load, and `render_game_to_text`. The inspected mixed-world capture is `output/playwright/agent-entities/archive-entity-playground.png`. Production TypeScript/Vite build passes; the required generic game client is clean under `output/web-game/agent-entities-first-pass/`.
- 2026-07-17 r10 adopts the complete-best-version rule: archive revisions are evidence for one canonical implementation, while genuinely distinct worlds and interaction surfaces remain separately counted. A read-only Datalake census identifies the winning lineages as the 2017 GraphysX engine, BallZ2015 six-screen flow, BallZ 2011 gameplay assets/skies, and the later Atmel/CubX animation branch; duplicate backups and vendor samples are excluded without dropping unique mechanics.
- BallZ18 contributes one new authored world, not six era-specific menu copies. `BallZ18 Level 01 — AI Circuit` is the 49th canonical scene and now lives in BallZ Tour with the exact SHA-256-locked eight-object `L1_floor.blend` export, both source starts, seven-point Raceline, 0.5-radius player, torque BallAI with the scene's 15-rad/s cap, exact half/lap segments, the source-local WoodFloor05 cube, and a repaired 3.5-second control lock. The Unity source has recurring lap splits but no terminal win, so its visible three-lap finish is explicitly the revival progression adapter; no rings or Standard Assets runner were invented.
- BallZ18 MainMenu and TrackSelect merge into the stronger Home/Tour, and MathGames merges into the stronger Math workbench. The exact Unity a=.01/b=0/c=100/m=5 formula preset is now visible beside separately labeled modern inspection presets. SerialComm and ArduinoCtrl map to one future safe Input & Device Lab rather than two diagnostic era scenes; simulation-by-default and opt-in device access remain acceptance requirements.
- The census, in-app registry, Home count, and tracker now agree at 49 records: 3 RESTORED / 46 PARTIAL / 0 REGRESSED / 0 PIPELINE / 0 MISSING. Production TypeScript/Vite build passes at visible build `revival-2026.07.17-r10`; focused BallZ18/Math merge QA passes 20/20 with zero errors and visually inspected Tour/live/results/Math captures under `output/playwright/ballz18-level01/`. The complete integrated matrix passes 263/263 with zero errors and build/census assertions under `output/playwright/r10-restoration-final/`. The required generic game client completed two clean r10 Home iterations under `output/web-game/r10-best-version-clean/`, emitted both state/canvas captures, and produced no error file.
- 2026-07-17 Agent World human-authoring parity implementation is now mounted in the existing Studio. It adds a complete entity outliner, select/update/remove inspector, transforms, visibility/tags/color/light controls, optional static/dynamic/kinematic physics and live velocity editing, all-type entity creation (primitive/group/light/model/spline), recovered model picker, spline-point editor, interaction JSON, behavior attach/detach, arbitrary snapshot names, complete-world JSON load/export, atomic transaction JSON, and revision-guarded human commit JSON. Every button delegates to the same `RaceScene -> AgentWorldRuntime` methods used by `window.__GRAPHYSX__`; no parallel editor state or renderer was introduced. Strict TypeScript passes; browser/visual/regression QA is next.
- Human + agent authoring parity verification is complete. `qa:agent-world-ui` drives the visible UI to apply a complete world; create and edit a dynamic primitive, static recovered model, spline, point light, behavior, and interaction; run an atomic transaction and revision-guarded human commit; verify failed-command rollback; then save, clear, and restore a named snapshot while checking every result through `window.__GRAPHYSX__` and `render_game_to_text()`.
- The first parity run exposed missing light observability in `query()`. `AgentWorldEntityState` now reports material, geometry, intensity, distance, and shadow flags in addition to transform, hierarchy, physics, model/path, behaviors, interactions, tags, and visibility. The final static-bundle browser run passes 34/34 with zero console/page errors.
- Visual QA confirmed the editor and 3D world remain readable together after switching the authoring forms to container-responsive stacking. Production Vite bundling succeeds; evidence and the structured report are under `output/playwright/agent-world-human-editor/`.
- 2026-07-17 r11 Datalake reconciliation corrects the surviving-interface record: BallZ2015 does retain six exact CEGUI layouts under `Codes/BallZ2015/Debug/GUI/layouts` (Splash, Main Menu, Set Race, After Race, Math Game and Editor). They are now treated as source evidence feeding the stronger canonical Home/Tour/Math/Editor flow instead of as absent layouts or six additional era-specific scenes. BallZ Tour visibly preserves the recovered race rules: Collect All Rings (chrono), 10 Laps (chrono and rings), and Free for All (five minutes).
- BallZ18 Level 01 now reproduces its exact countdown contract: a half-second initial delay, `beepShort.mp3` for 3/2/1 one second apart, and `beep01.mp3` at GO. The two source files are byte-identical Datalake copies and the runtime exposes source-aware countdown telemetry. BallZ2015 ring pickup and jump likewise use exact `coin.wav` and `Jump.wav` instead of synthesized substitutes. `intro.mp3` and `Piano.mp3` were inventoried but deliberately not enabled until their music provenance is reviewed.
- Math Game now uses the exact six-face 1024×1024 BallZ2015 `NightSky` BMP cubemap. This replaces the mismatched lower-fidelity presentation while retaining the earlier source audit's honest boundary: the surviving Math branches contain visualization formulas and serial/device controls, but no proven goals, score, success/failure or progression loop.
- r11 adds the canonical safe Input & Device Lab, merging BallZ18 SerialComm/ArduinoCtrl and related Scene3D/Atmel device surfaces into one best-version destination. It starts in Simulation at 9600 baud and disarmed; ARM/STOP gates all commands. Five documented profiles cover the PhysX four-byte robot protocol, BallZ18 `COMOK`, Scene3D five-byte serial commands, MeArm text control and AtmelCubx ASCII/raw. The lab includes live BallZ18-style raw input monitoring with the exact 0.19 deadzone, robot commands 2/4/6/8, pin-8 value 127, identify variants, single sonar, the complete 182-point forward/reverse sweep, eight D/E I/O channels, four C/alarm schedules, repaired/clamped MeArm controls and a bounded timestamped TX/RX log. Exact controller/open/closed source graphics are used where they survive; real device access remains an explicit future opt-in boundary.
- The renewed read-only Datalake scene hunt found three genuinely distinct best-version experiences that were not duplicate snapshots: the browser-native Three.js Playground (`Codes/SBQC/public/Projects/3D Playground`), the separate BallZ/Blender Level 1 geometry (`blenderModel/Levels/Level1.blend` and Unity `BallZ/Assets/Models/Level1.fbx`), and the Maison Explorer family (`blenderModel/Maison/maison.blend` plus `Cuisine.blend`). They enter the ledger honestly as `PIPELINE`; `InteriorBase.blend` remains research reserve, Scene1/Scene2 are Suzanne authoring lineage, and `AtmelCubx63_2003.rar` enriches CubX rather than creating another scene.
- The reconciled canonical census is now 53 reachable/planned records: 3 `RESTORED`, 47 `PARTIAL`, 0 `REGRESSED`, 3 `PIPELINE`, 0 `MISSING`, 0 `RESEARCH`. The increase is one reachable Input & Device Lab plus the three newly proven pipeline scenes. Duplicates, vendor samples, serialization fixtures and evolutionary UI snapshots remain excluded under the complete-best-version rule.
- r11 verification is clean. `npm run build` passes at visible build `revival-2026.07.17-r11`; Input & Device Lab QA passes 31/31; BallZ18/audio/NightSky QA passes 28/28; the complete integrated matrix passes 268/268 with zero console/page errors under `output/playwright/r11-restoration-final/`. The required generic development client completed two clean r11 Home iterations under `output/web-game/r11-best-version-clean/`, with structured state and inspected canvas captures and no error file.
- The next evidence-led restoration order is: (1) mount the complete Three.js Playground as its own Archive World; (2) convert the distinct BallZ/Blender Level 1 geometry into a source-bounded visit without inventing a race; (3) merge Maison and Cuisine into one explorer with house/kitchen subspaces; (4) replace more procedural CubX shell pieces with the recovered TVM/3DS/icon/8K-Earth assets; (5) validate BallZ18's clear-sky orientation before adopting it. Unproven Metal03-to-Level3 association, particle preset gameplay binding and lost authored Dominus composition remain explicit evidence boundaries.
- 2026-07-17 r12 immediately completes the first newly proven Datalake port: Three.js Playground is now a first-class Archived World rather than a registry promise. The source at `Codes/SBQC/public/Projects/3D Playground` contributes one authored browser composition; its hash-identical iGrow copy remains an alias. Nine runtime assets totaling 3,712,102 bytes are copied byte-identically and hash-locked: `Airplane.glb`, `twoway.jpg`, `earth_uv_with_topo.jpg`, and all six Asteroids sky faces.
- `ThreejsPlaygroundEnvironment` runs the complete recovered composition together: the 10× Airplane GLB follows its radius-5 time formula; three radius-2/detail-5 icospheres execute the exact archived vertex/fragment shader math; Earth/Mars/Moon share the source Earth texture and positions; the red 10×10/121-vertex procedural plane is raycastable; the textured cube and three-point blue line survive; and the hemisphere/spotlight rig retains the source 240-frame period, amplitude 75, and offset 73.
- Presentation adapters remain explicit. The unusable source-origin camera receives a reversible 55° inspection orbit, the vertical local-Z plane is rotated onto XZ, and `Math.random` terrain is seeded for deterministic QA. Full dat.GUI parameter parity and the original FPS widget remain fidelity work, so the scene advances only `PIPELINE -> PARTIAL`, never `RESTORED`.
- Home and the compact mode switcher now open the Playground directly; A/D, drag, wheel, visible orbit/reset buttons, pointer-driven shader input and click terrain raycasts share the integrated renderer. `render_game_to_text()` and `__GRAPHYSX_DEBUG__` expose source hashes, camera, airplane, shader/planet/terrain/sky/light state and adapter disclosures without leaking BallZ player/race state into the visit.
- The canonical census remains 53 but is now 3 `RESTORED`, 48 `PARTIAL`, 0 `REGRESSED`, 2 `PIPELINE`, 0 `MISSING`, 0 `RESEARCH`. The only newly proven scene ports still queued are BallZ / Blender Level 1 and Maison Explorer; InteriorBase stays a research reserve rather than an inflated scene record.
- r12 verification is clean: the isolated Playground suite passes 33/33, production TypeScript/Vite build passes, and the full integrated matrix passes 279/279 with zero console/page errors under `output/playwright/r12-restoration-final/`. The required generic client completed two clean r12 Home iterations under `output/web-game/r12-datalake-playground-clean/`; the integrated Playground capture and client canvas/state were visually inspected, and no error file was produced.
- 2026-07-18 Agent Authoring 1.0 implementation is underway. Stable entity IDs now drive direct viewport selection and Three.js TransformControls for snapped move/rotate/scale; mouse release commits through the ordinary v2 `update()` path, while W/E/R switches tools and the visible inspector mirrors runtime selection. Interactive objects retain their original click action while becoming selected.
- The World Editor now has real portable-file controls: Download JSON emits the complete `graphysx.agent-world/v2` definition and Import JSON validates a selected file through the same atomic `create()` path with a 5 MB safety limit and visible errors.
- Added the lightweight `graphysx.agent-tool-bridge/v1`: `window.__GRAPHYSX_AGENT_BRIDGE__` exposes a discoverable manifest, direct async calls, same-origin `window.postMessage` request/response transport, and state-change notifications over the existing world/level API. No server or duplicate runtime was introduced.
- Agent Authoring 1.0 is now verified end to end. The focused human/agent parity suite passes 53/53 with zero browser errors, including bridge discovery/direct calls/structured errors/same-origin messaging/events, portable JSON download/import, agent-driven shared selection, transform mode/space shortcuts, and a physical drag of the rendered gizmo that commits a snapped v2 transform.
- Core regressions remain clean: Agent World passes 69/69 (including direct 3D click selection), Agent Entities passes 26/26, and the named multi-level/ASCII editor passes 38/38 after a clean live-source-server run. Strict TypeScript plus the 109-module production Vite build pass. The required generic development client produced a clean World Studio state/canvas capture under `output/web-game/agent-authoring-1-first/`, and the selected-world/gizmo/editor captures under `output/playwright/agent-world-human-editor/` were visually inspected.
- Public guidance now documents portable files, shared selection/gizmos, the discoverable `graphysx.agent-tool-bridge/v1`, direct async calls, structured requests, same-origin `postMessage`, and state-change events. The recovery tracker now reflects which editor functions are genuinely restored versus the smaller material-browser, friendly trigger-composer, NPC contract, and legacy XML migration work that remains.
- 2026-07-18 Agent World texture increment begins from the user's clarified product direction: GraphysX is an agent's manipulable 3D thinking/physics space and the shared editor is how a person communicates spatial information back to that agent. Added a deliberately small semantic library of eleven recovered textures (checker, grid, abstract cubes, Two Way, two metals, marble, two woods, Earth, and a sphere study) with stable IDs, descriptions, source family, URLs, and repeat defaults.
- `AgentWorldMaterial.texture` now carries stable ID plus repeat/offset/rotation through validation, query/state, JSON export/import, snapshots, and updates. The renderer asynchronously caches source textures, clones per-material transforms, preserves native model maps when no override is requested, and disposes only agent-owned texture instances. `gx.textures()` and the discoverable bridge `textures` tool expose the same library to agents; strict TypeScript passes.
- The shared World Editor now has one compact visual material/texture browser in both Create and Selected Entity flows, with thumbnail selection plus color tint, emissive/glow, roughness, metalness, opacity, repeat, offset, and rotation. Texture buttons update ordinary form values; Apply/Create still goes through the same validated runtime used by agents.
- 2026-07-18 stewardship/release pass begins: play-test the complete current resurrection as one release candidate, tune only issues reproduced in the running app, finish with a clean production build and intentional Git commit, then inspect the available deployment path for `specialblend.ca`.
- 2026-07-18 r13 closes every scene-level port proven by the preceding Datalake hunt. Three.js Playground now preserves all 16 original dat.GUI controls plus the archived FPS widget and advances `PARTIAL -> RESTORED`; focused browser QA passes 43/43 with zero console/page errors.
- The distinct BallZ/Blender Level 1 and Maison/Cuisine family are now mounted as source-bounded archive visits. Their exact source hashes, deterministic Blender 2.79 FBX exports, authored geometry/material slots, source-camera evidence and disclosed presentation cameras are preserved; focused browser QA passes 34/34 with zero errors. Both advance `PIPELINE -> RESTORED` without inventing race rules or house interaction.
- A follow-up read-only Datalake sweep found one additional authored composition worth reviving: Unity `Arena`. Its byte-identical OBJ/MTL/material/1024 atlas, source transform, source camera and directional-light evidence now form the 54th canonical scene. The default overview and exact source-camera toggle are explicitly separated; Arena QA passes 18/18 with zero errors.
- The same sweep deliberately excludes non-canonical material rather than inflating the scene count: Unity Boids, tutorial kits, 3D Game Kit and Kart are upstream/vendor training; SBQC `3D Scenes` is a named examples/tutorial collection; BaseWorld and Scene1/Scene2/FaceStart are primitive/Suzanne authoring exercises; BallZ HuaYing/SmallOrc are third-party; MathGame is the already merged runnable build; and the CubX RARs enrich the existing CubX/CubZ restoration rather than prove another world. The 4 GiB `Unity Projects.zip` listing exposed no new usable authored scene source.
- The reconciled canonical census is 54 records: 7 `RESTORED`, 47 `PARTIAL`, 0 `REGRESSED`, 0 `PIPELINE`, 0 `MISSING`, 0 `RESEARCH`. Here `PARTIAL` means a visible, documented source-fidelity or hardware boundary in an already reachable best-version experience; it does not mean a forgotten first port. Missing archive evidence remains a boundary, never an invitation to fabricate content.
- 2026-07-18 stewardship verification is clean on an immutable production snapshot of the latest tree: full r13 matrix 293/293; shared human/agent World Editor 73/73; Agent World API 69/69; agent entities 26/26; named level API 38/38; camera collision 14/14; input/accessibility 15/15; all with zero browser/page errors. The required generic game client drove Archive Level 1 through live start, forward/turn/jump bursts, advanced the player from the authored start to the far course boundary, captured four inspected gameplay frames/states under `output/web-game/stewardship-final-ballz/`, and emitted no error file. `npm run build` passes with only Vite's existing large optional-chunk advisory.
- Stewardship kept gameplay tuning evidence-led: no speculative physics/camera constants were changed after the clean play and regression passes. The full r13 harness now records failed request URLs as well as console errors, which distinguished one moving-`dist` hashed-chunk failure and transient local-server timeout from application defects. The repository ignore list now excludes the two one-off fidelity build mirrors while preserving source assets and QA captures.
- The final shared-authoring reconciliation adds the 16-entity Physics Sketchbook starter, a visible serializable `agent` entity, real typed `apply-impulse` interactions, the eleventh recovered Two Way texture, and archived GraphysX serializer XML migration with explicit proxy warnings. The public bridge exposes the same migration/texture/selection/level tools and the human editor uses the same runtime.
- Added `tools/graphysx-agent-stdio.mjs`, a deliberately small Playwright-backed JSON-lines adapter for an external LLM/tool process. It discovers the browser bridge manifest, supports one-shot structured requests, request files, and persistent `--stdio` sessions, and keeps the browser runtime as the only source of world truth. `npm run agent:manifest` and a real external `textures` request both pass against the live app.
- Final verification against a fresh static production server is clean: Agent World passes 69/69, the expanded human/agent editor passes 73/73, focused Playground/Blender/Arena suites pass 43/43 + 34/34 + 18/18, and the complete r13 archive matrix passes 293/293; every browser suite reports zero console/page errors. Strict TypeScript and the 113-module production build pass. The required generic game client completed two ready-state iterations at visible build `revival-2026.07.18-r13` under `output/web-game/r13-completion-final/` with no error file, and its canvas capture was visually inspected.
